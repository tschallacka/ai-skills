# MODE: PROD
# What this skill needs on the target machine at runtime, and how badly.
#
# installer/build.sh generates install.sh's dependency tables from every skill's
# copy of this file; the shared installer/tools.tsv says how to verify a tool and
# how to install it, so the install instructions are written once, not per skill.
#
# Columns, tab-separated:
#   tool       tool id; must have a row in installer/tools.tsv
#   condition  <uname -s glob>:<uname -m glob> the row applies to, e.g. *:* or
#              Darwin:arm64 — a bash case pattern, so | may join alternatives
#   strength   hard = the skill does not work without the tool, so without it
#              the installer refuses to install this skill and exits non-zero
#              soft = the skill degrades honestly, so the installer installs it
#              and warns which capability is lost
#   why        one line naming the capability that is lost without the tool
#   group      optional any-of group id: the requirement is satisfied by ANY
#              row sharing the id, so the installer warns once for the set
#              instead of once per member. Ids must be unique across skills.
#
# Line-oriented TSV rather than JSON on purpose: jq is itself declared here, so a
# format that needed jq to read it could not be read on the machine that is
# missing it. awk and sed parse this.
tool	condition	strength	why	group
bash	*:*	hard	every helper this skill ships is a bash script, so without bash none of them run; the guidance in SKILL.md still reads fine
jq	*:*	hard	reads the placeholder and state-change registries and edits agent permission config; validate-plan.sh refuses to run without it
python3	*:*	soft	serve mode is unavailable without a local overview runtime	overview-runtimes
node	*:*	soft	serve mode is unavailable without a local overview runtime	overview-runtimes
perl	*:*	soft	serve mode is unavailable without a local overview runtime	overview-runtimes
socat	*:*	soft	serve mode is unavailable without a local overview runtime	overview-runtimes
