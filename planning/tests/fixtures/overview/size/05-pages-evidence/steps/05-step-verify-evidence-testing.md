# Verification: 05-step-verify-evidence

## Automated tests

§ 2.1
Open every test row in the fixture and confirm each leads to a page rather than a dead end, recording the count. This is the interaction that failed in the previous renderer with 20 dead rows, so the recorded count must equal the number of rows.