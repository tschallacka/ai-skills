# Verification: 07-step-binaries-contract-test

## Automated tests

§ 2.1
Run tests/test-shipped-binaries.sh after W110, W113 and W115. Verify registry-to-tree, tree-to-registry and registry-to-installer equality, then apply missing, extra and installer-mismatch mutations and require each to fail.
