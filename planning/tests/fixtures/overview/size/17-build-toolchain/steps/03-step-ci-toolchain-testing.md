# Verification: 03-step-ci-toolchain

## Automated tests

§ 2.1
Run the CI crate leg with rustc 1.86.0 and require a non-zero crate test count. Remove the installation step in a fault injection and require the job to fail.