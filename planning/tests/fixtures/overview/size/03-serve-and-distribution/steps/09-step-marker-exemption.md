# Step: 09-step-marker-exemption

## Ownership

- Goal: `03-serve-and-distribution`
- Work unit: `W76`
- Type: `test`

## Change target

- File: `tests/test-mode-markers.sh`
- Primary symbol or file scope: `exempt()`
- Subscope: `N/A`

## Objective

§ 4.1
Add the exemption arm the shipped prebuilt artifacts need, with a stated reason, and remove the now-dead arm exempting the deleted runtime directory. A compiled file carries no comment syntax, so it can hold no marker; a stale arm exempting paths that no longer exist hides the next real violation. The Cargo.lock arm already exists and is not added here. The crate sources and the stylesheets are not exempt: they carry markers, read by the forms W109 makes available.

## Instructions

§ 5.1
In the exempt function of tests/test-mode-markers.sh, add one arm covering the shipped prebuilt artifacts with a printed reason, and remove the arm that exempts the three planning/scripts/runtime interpreter payloads, whose directory W16 deletes. A compiled artifact carries no comment syntax, so it can hold no MODE marker; the reason must say that rather than merely listing the path. Do not add a Cargo.lock arm: one already exists, printing that the file is generated by cargo and rewritten on any dependency change, and compiler_input() already carries the crate source patterns. Three earlier versions of this instruction were wrong: the first exempted the crate sources, which CODE-STYLE section 1b requires to carry MODE: DEV and PACKAGE: PROD; the second cited the runtime arm as line 74, which is the json and jsonl arm; the third claimed the Cargo.lock arm as work to be done when it was already there. Adversarial findings AR-16, AR-26 and AR-35 recorded them in turn.

## Acceptance criteria

§ 6.1
The marker gate passes with the artifacts present, and no exemption arm names a path absent from the tree, checked by listing each arm's pattern against the tracked file list. The gate still fails for an ordinary script with no marker. The proofs that a crate source and a stylesheet still fail when their markers are stripped belong to W108, which runs after those files and the CSS marker form exist: an earlier version of this criterion demanded them here, while W64 to W66, W109 and W19 are all downstream of or unordered against this unit, so none of the three was reachable. Adversarial finding AR-35 recorded it.

## Handoff

§ 7.1
W20 can rely on the marker gate accepting the shipped artifact set, so the manifest work is not blocked by a gate that cannot describe a binary.

## Atomicity check

- [ ] This step owns exactly one inventory work unit.
- [ ] No other file, symbol, test target, or verification flow changes here.
- [ ] Any follow-on target has a separately named work unit and step.
