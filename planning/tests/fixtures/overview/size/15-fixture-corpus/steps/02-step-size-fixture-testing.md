# Verification: 02-step-size-fixture

## Automated tests

§ 2.1
Confirm the recorded byte size matches the state document exactly. Reproduce the current failure against the snapshot and record its exit status and message, so the before state is pinned to this fixture rather than to a live plan.