# Verification: 03-step-derive-counts

## Automated tests

§ 2.1
Run the derive tests, then for one fixture enumerate each counted collection independently and compare the totals against the derived values; any mismatch is a failure.