# Verification: 01-step-render-shell

## Automated tests

§ 2.1
Render the size fixture, confirm one artifact containing every page and the embedded state, and record peak memory plus the number of output buffers used. Open the artifact from disk to confirm the state is present.