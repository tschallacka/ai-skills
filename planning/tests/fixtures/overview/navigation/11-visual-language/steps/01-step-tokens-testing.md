# Verification: 01-step-tokens

## Automated tests

§ 2.1
Confirm every colour, spacing and type value used by a page resolves to a declared token, and that no page declares a colour outside the token scope. Then render in light and dark and confirm both resolve.