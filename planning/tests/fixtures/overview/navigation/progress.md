# Progress: plan-overview-rebuild

**Overall progress:** `0%  --------------------  100%` 💤

| Goalname | Description | Completion status |
|---|---|---|
| 01-engine-core | A Rust binary reads a plan tree of any size and produces the full derived state in memory: identity,... | 💤 incomplete |
| 02-page-shell | One artifact containing every page, routed by URL hash, with breadcrumbs, a back-stack, and an inlin... | 💤 incomplete |
| 03-serve-and-distribution | The Rust binary compiles from the pinned toolchain, serves the artifact itself and is the only rende... | 💤 incomplete |
| 04-pages-primary | The monitor page shows live state and what moved; goal and unit pages show one thing in full; every ... | 💤 incomplete |
| 05-pages-evidence | Findings, tests and coverage each get a page whose content is the evidence itself: a test shows what... | 💤 incomplete |
| 06-pages-history | A history page presents the plan's evolution: status transitions, findings superseded and by what, w... | 💤 incomplete |
| 07-graph-and-coverage-of-data | A graph page presents units as nodes in dependency order with status as colour and edges as links, s... | 💤 incomplete |
| 08-autoplay | An opt-in autoplay mode navigates to the step the agent is working on, driven by the served state ra... | 💤 incomplete |
| 09-verification | Every page has passed a browser story with real interaction on the 82-unit fixture, the 337 KB fixtu... | 💤 incomplete |
| 10-live-updates | A file monitor on the plan directory drives everything live: the served page updates without a reloa... | 💤 incomplete |
| 11-visual-language | One design system the pages share: tokens for palette, luminance and type scale; a named depth scale... | 💤 incomplete |
| 12-adaptive-cinematics | The page measures its own frame times and selects a cinematic tier from a single declared table, ste... | 💤 incomplete |
| 13-removal-declared-surface | Every file that declares, ships or documents the old renderer is corrected, so nothing points at som... | 💤 incomplete |
| 14-removal-test-surface | Every test that exercised the old renderer is retired or rewritten against the binary, so the suite ... | 💤 incomplete |
| 15-fixture-corpus | Every state a story or a work unit needs to observe exists in a fixture checked into the repository,... | 💤 incomplete |
| 16-toolchain-and-packaging | What ships is declared once, validated against the tree, and reaches a host as exactly the one artif... | 💤 incomplete |
| 17-build-toolchain | The compiler that builds the crate is pinned to one floor version and present wherever the crate is ... | 💤 incomplete |
