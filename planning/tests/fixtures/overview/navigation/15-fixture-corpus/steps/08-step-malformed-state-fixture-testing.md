# Verification: 08-step-malformed-state-fixture

## Automated tests

§ 2.1
Confirm the state document is truncated in the recorded place and one transition has no time. Run every tool that walks the fixture tree and confirm none of them fails because of this directory. Render the fixture and record verbatim what the page does with it, whatever that turns out to be.