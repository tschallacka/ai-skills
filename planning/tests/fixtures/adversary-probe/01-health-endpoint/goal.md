# Goal: Add the /health endpoint

## Objective
Return `{"status":"ok"}` from a new `/health` route with HTTP 200.

## Owned work units
- W01 — Add the route handler in app.js.
- W02 — Add a passing test in test/app.test.js.
