# Verification: 05-step-add-adapter-injection-seam

## Automated tests

Run the adapter with the injected fake reviewer command and separately verify the default command path remains unchanged; assert deterministic output and no live codex invocation.
