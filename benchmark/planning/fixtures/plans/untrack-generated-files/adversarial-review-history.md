
## Cycle 1

| ID | Missing or over-broad item | Required plan change | Status | Work unit |
|---|---|---|---|---|
| AR-01 | No finding recorded yet. | N/A | ✅ resolved | N/A |

## Cycle 2

| ID | Missing or over-broad item | Required plan change | Status | Work unit |
|---|---|---|---|---|
| --- | --- | --- | open |  |
| AR-1 | "No work unit establishes the local rjq acquisition path (""the documented bootstrap""), yet after W01 untracks the blob, `npm pack` (prepack runs `tests/test-register-schemas.sh`, package.json verified: `prepack: tests/test-skill-files-manifest.sh >/dev/null && tests/test-register-schemas.sh >/dev/null`), W06 probe step (3), W16, W31 and W32 all need rjq on PATH. W08's bootstrap builds only the five libs; W11 §5.1 explicitly rules prepack out (""a host precondition ... not something prepack can build""); plan §10.1 rejects a Rust toolchain only ""at install time"", so a dev-time `cargo build -p rjq` (crate exists at src/rjq) is available but unplanned. Risk (3) promises ""local runs use the documented bootstrap"" but no unit documents or implements it; grep of DEVELOPMENT.md/CONTRIBUTING.md/README.md found no rjq/cargo acquisition docs either." | "Add a work unit in goal 02 that documents (and ideally scripts) the local rjq bootstrap (e.g. build src/rjq into a PATH dir, or nix-provided), reference it from W14's AGENTS.md text and from risk (3); state what the bootstrap delivers (libs, REVIEWER.md, rjq) so ""one documented bootstrap command"" in §3.1 is real." | open | W37 |
| AR-2 | "Risk (3)'s promised behaviour — ""the suite must fail with that instruction, not with a bare command-not-found"" — has no owning work unit. Verified: tests/test-register-schemas.sh calls `rjq` bare throughout (lines 25, 41-46, 67-116) with no `command -v` guard, so post-untracking it fails with command-not-found; tests/test-rjq-active-references.sh:27-28 prints its own message but no unit reconciles its skip/fail semantics. Neither file appears in the inventory." | Add a work unit (goal 06 fits the packaging-adjacent-gates charter) to add an rjq-availability guard to tests/test-register-schemas.sh (and confirm test-rjq-active-references.sh's existing message) that fails naming the AR-1 bootstrap instruction; cross-reference AR-1. | open | W38 |
| AR-3 | "Dependency edge missing: W06's probe (2) runs install.sh against a scratch tree without the binary and asserts ""the scratch install exits 0 with the notice"", but that behaviour is produced by W34 (install_skill() skip-copy + notice, ""instead of failing the install on cp of a missing file""), which W06 does not depend on (W06 deps: W02,W03,W04,W05; W34 deps: W01,W03). Executed per the recorded edges, W06 can run before W34 and its acceptance criterion fails by construction — the install probe exercises exactly the copy path W34 fixes." | "Add W34 to W06's Depends-on (inventory row, goal roster §9.6/§9.7 order note, and W06 step §5.1 precondition) so the install probe runs after the copy-skip fix." | open | W06 |
| AR-4 | "Goal-ordering contradiction: goal 05 (W31 ""full suite ... to green""; W32 §6.1 ""test-release-package.sh passes in the working tree"") executes before goal 06, but the full suite includes tests/test-release-package.sh and tests/test-mode-markers.sh, whose committed-copy assertions the plan itself says break when libs and REVIEWER.md are untracked (plan §2.1: ""property 2 at line 113, which breaks when libs and REVIEWER.md are untracked""). W31's deps (W06,W16,W24,W26..W30) and W32's deps (W11,W12,W21,W31) omit W35/W36; W35/W36 have no dependents. Either the tests/ gates are suite members — then W31/W32 fail by construction as ordered — or they are not, and the ""full suite green"" proof does not cover the packaging gates it claims to prove." | "Either move goal 06 (W35, W36) before goal 05 and add W35,W36 to W31's and W32's Depends-on, or state in W31/W32 that the tests/ gates run outside run-tests.sh and add an explicit execution command for them; record whichever suite-membership fact is true (repo not re-verified — batch cap)." | open | W31 |
| AR-5 | "RELEASE.md is named in approach §4.1 (""AGENTS.md, DEVELOPMENT.md, RELEASE.md and CODE-STYLE.md wording moves from is-fresh to regenerate-on-demand"") but has no work unit (W05/W14/W15/W23/W29/W30 cover MAINTAINER/AGENTS/DEVELOPMENT/CODE-STYLE only) and is absent from affected areas §6.1 — the plan contradicts its own approach section." | "Add a docs work unit in goal 04 for RELEASE.md's PORTABILITY.md wording, or strike RELEASE.md from approach §4.1 if the file carries no such wording (existence/content not verified — batch cap)." | open |  |
| AR-6 | "Verification preconditions unstated: W16/W31 build the scratch tree with `git archive HEAD` and W33 runs ""against a tag checkout"", but git archive/tag only show the four classes untracked and the new gates present after goals 01-04 and 06 are committed to HEAD; run from a staged-but-uncommitted tree (as MAINTAINER.md §2.15 is today), W31's `git ls-files` assertion is unmeetable. No step states the committed/tag precondition." | "Add to W16/W31/W33 §5.1 an explicit precondition: all prior goals merged and committed to the branch HEAD (and for W33, a tag that contains them, or run against HEAD with a temp tag); name which commit/tag the probe archives." | open | W31 |
| AR-7 | "All six goal documents carry unfilled template placeholders — §2.1 `<confirmed facts and prerequisite handoffs>`, §4.1 `<how this goal contributes to the initiative>`, §5.1 `<included and explicitly excluded behavior>`, §6.1 `<concrete affected areas>`, §7.1 `<prerequisites and precise downstream handoffs>`, §8.1 `<approach, risks, and edge cases>` — and every goal's Testing-requirement row is `no` with the template rationale `<set to yes when this goal has a testable behavior...>` despite each goal owning test-type units (W02/W04; W09/W10; W18; W26; W35/W36). Goal-level §7.1 ""Dependencies and handoffs"" is where cross-goal handoff belongs; it is empty in all six." | "Fill §2.1/§4.1/§5.1/§6.1/§7.1/§8.1 per goal with the goal's own scope, affected files, dependencies and handoffs (content exists in the plan description and inventory — it needs to reach the goal surfaces), and set Testing-requirement honestly (yes where the goal ships test behaviour)." | open |  |
| AR-8 | "W08's run-tests bootstrap covers only the five libs; no unit makes the suite generate REVIEWER.md, yet planning/tests are unverified-clean of readers of the tree copy planning/REVIEWER.md (e.g. planning/tests/test-persona-drift.sh, which per role docs asserts registry/shipped scope sync involving REVIEWER.md — not verified within the batch cap). W36 adds generate-if-missing for one test only. If any planning/tests file reads planning/REVIEWER.md from disk, W16/W31's clean-checkout green suite fails after W17 untracks it." | "Extend W08 (or add a sibling unit) to generate-if-missing REVIEWER.md before suite discovery (the W36 pattern), or record verified evidence that no suite test reads the tree copy; state it in W16/W31's criteria." | open | W08 |
| AR-9 | "W20's premise overstates the defect: planning/scripts/plan-context-lib.sh:343-344 already guards with `[ -f ""$CONTEXT_SOURCE_ROOT/planning/REVIEWER.md"" ]`, so an absent REVIEWER.md is silently omitted today — nothing ""lists a path and hash that do not exist"". The real change is narrower: add the stated reason line." | "Reword W20's objective/inventory description to ""add a stated reason line when omitting the absent REVIEWER.md entry"" so the step does not read as fixing a broken listing." | open | W20 |
