# Adversarial review — incoming findings (fresh-review mode, protocol 1.4.2)

Reviewer: christoph (Reviewer B persona, cycle-2 fresh-review pass). Session: opencode-639727.
All plan reads via plan-context.sh gate (ROLE_ID=christoph). Findings numbered AR-10+ to follow the closed AR-1..AR-9 already recorded in adversarial-review.md.

| ID | Missing or over-broad item | Required plan change | Status | Work unit |
|---|---|---|---|---|
| AR-10 | Missing dependency edge: W08's rjq arm invokes bootstrap.sh, which only W37 (goal 07) creates, but W08's Depends-on lists only W07 (inventory row, goal 02 § 9.2). run-tests.sh:5 runs tests/ too, where tests/test-register-schemas.sh:25 calls bare `rjq` and nothing prepends planning/bin to PATH — so between goal 01 (blob untracked) and goal 07 (bootstrap lands) the rjq arm and the register tests cannot pass on a clean checkout, yet goal 02 is sequenced before goal 07. | Add W37 to W08's Depends-on (inventory row + goal 02 § 7.1/§ 9.2 edges), or sequence goal 07 before goal 02 via recorded edges; state in W08's criteria how the arm behaves while bootstrap.sh does not yet exist mid-migration. | open | W08 |
| AR-11 | Cross-ref AR-10: W16's criterion "the full suite is green in the scratch tree" is unpassable at its goal-02 position — a git-archive checkout carries only tracked files, so it has no rjq after goal 01 and no bootstrap.sh until W37 (goal 07); W16's Depends-on (W08,W09,W10) omits W37/W38, so the verification would run before what it verifies. | Add W37 (and W38) to W16's Depends-on, move W16 after the rjq chain, or narrow its criterion to the lib surface until the chain lands. | open | W16 |
| AR-12 | RELEASE.md is named in plan scope § 5.1 among the docs stating the committed-file contract, but no work unit owns it (W05/W14/W15/W23/W29/W30 cover MAINTAINER.md/AGENTS.md/DEVELOPMENT.md/CODE-STYLE.md only). RELEASE.md:46-52 step 4 instructs running `./planning/scripts/build-plan-libs.sh --check` and `./generate-portability.sh --check` against the tree, and miscounts "the four plan-*-lib.sh" (five groups since the crypt split: build-plan-libs.sh:87-91, MAINTAINER.md § 2.8). After untracking, step 4's committed-file --check flow breaks with no owning unit. | Add a RELEASE.md docs unit, or widen W15's inventory row and step file to name RELEASE.md explicitly, covering step 4's regeneration flow and the four→five count fix. | open | W15 |
| AR-13 | build-plan-libs.sh's --check mode is committed-file-bound ("--check compare the committed libraries against a fresh PROD build", build-plan-libs.sh:16,53,80-81) and test-plan-libs-build.sh drives through it (:39,:49,:147). W09 rewrites only the test and says "keep the --check teeth probe semantics against that fresh comparison" — impossible while --check still compares committed libraries, and no unit owns the --check redefinition (W27 covers only generate-portability.sh). Post-untrack, a stale local lib (the normal state once untracked) makes --check report a false "stale library". | Widen W09 (or add a sibling unit) to redefine build-plan-libs.sh --check as a two-fresh-build determinism compare mirroring W27, update MAINTAINER.md § 2.8's "--check reports a stale library" wording (:146-147), and restate W09's criterion to match. | open | W09 |
| AR-14 | package.json files:31 names "planning/bin" and .npmignore has no planning/bin exclusion, so npm pack ships any on-disk planning/bin content (files entries override ignore rules; because .npmignore exists, .gitignore is not consulted for packing) — while W37/W08 deliberately place rjq at planning/bin/<triple>/rjq on dev trees, making pack output machine-dependent. W11 pins "the files list untouched" and W06/W32 assert "no planning/bin path", which cannot hold on a tree where the bootstrap ran. | Widen W11 (or add a unit) to drop/negate planning/bin in package.json's files list — or record the explicit decision that npm never ships rjq and restate the W06/W32 assertions to hold regardless of local build state. | open | W11 |
| AR-15 | Goal 01 § 2.1 claims "installer/src/20-runtime-tools.sh prepend_bundled_rjq() prepends the triple dir unconditionally today" — contradicted by installer/src/20-runtime-tools.sh:90-93, where the prepend is already guarded on `[ -x "$SOURCE_ROOT/planning/$artifact" ]` and silently skips when absent. W03's instruction "guard prepend_bundled_rjq()" re-does master's work; only the stderr notice is new (same drift class risk 5 records for W02, not applied to W03). | Reword goal 01 § 2.1 and W03's objective/instructions to "the guard exists (master b0e31f7); add the notice to the absent branch"; keep the present-path byte-identity criterion. | open | W03 |
| AR-16 | W38 instructs "confirm tests/test-rjq-active-references.sh's existing unavailability message names the same fix", but the existing message names no fix (tests/test-rjq-active-references.sh:27-29: "rjq is not on PATH", exit 1). Goal 07 § 6.1 pre-authorizes the wording edit, yet W38's inventory row ("confirm") and acceptance criteria never state the message change, so the criterion fails by construction. | State in W38's instructions and acceptance criteria that the message must be edited to name bootstrap.sh + the T70 release note (as goal 07 § 6.1 already allows), with a criterion asserting the new wording. | open | W38 |
