# Goal: Untrack PORTABILITY.md; its gate regenerates to temp

## Current state and prior-goal handoffs

§ 2.1
PORTABILITY.md is generated by ./generate-portability.sh from portability-rules.json plus a PORTABILITY() marker scan; it requires rjq on PATH. Freshness is asserted by planning/tests/test-portability-contract.sh (which reports UNCONFIGURED without rjq) and by coupling.tsv rows run through blast-radius.sh. No prior-goal coupling beyond goal 01's rjq work.

## Outcome and definition of done

§ 3.1
PORTABILITY.md is gitignored and untracked; planning/tests/test-portability-contract.sh regenerates to PORTABILITY_OUTPUT temp paths for the freshness arm while marker-id and banned-construct sweeps run unchanged against the regenerated text; coupling.tsv rows 6-7 flip to the same temp-regeneration check; AGENTS.md and CODE-STYLE.md wording moves from is-fresh to generate-to-read. Demonstrable: rm PORTABILITY.md, run the portability contract test and blast-radius.sh --check paths - both pass by regenerating, and both still fail when portability-rules.json and the markers disagree.

## Why this goal is needed

§ 4.1
The catalogue is repo-internal (never packaged), so untracking it changes no delivery surface - but its gate must keep detecting rule-versus-marker drift, which is what the temp-regeneration rewrite preserves.

## Scope

§ 5.1
In: the .gitignore entry, the contract test's freshness arm, the generator's --check determinism mode, the coupling.tsv rows, and AGENTS.md and CODE-STYLE.md wording. Out: portability-rules.json content, the marker policy itself, and any consumer that reads the catalogue's rules rather than the file.

## Affected files, systems, data, and interfaces

§ 6.1
.gitignore; planning/tests/test-portability-contract.sh; generate-portability.sh; coupling.tsv; AGENTS.md; CODE-STYLE.md.

## Dependencies and handoffs

§ 7.1
Depends on goal 01 (rjq must be acquireable for the generator). AGENTS.md edits here touch different sections than goal 02's, so no collision. Handoff: W31 re-runs the contract test in the whole-plan proof.

## Implementation approach, risks, and edge cases

§ 8.1
The UNCONFIGURED-without-rjq path is preserved exactly so a missing toolchain still reads as unconfigured, not failed. Edge: blast-radius.sh executes the new check command via coupling.tsv - test-blast-radius.sh's parser assertions must keep passing, so the row format is validated in W28's criteria.

## Owned work units

§ 9.1
`W25` — Add PORTABILITY.md to .gitignore with a comment naming generate-portability.sh and portability-rules.json as the producer.

§ 9.2
`W26` — Rewrite the freshness arm: regenerate to PORTABILITY_OUTPUT temp paths and byte-compare two fresh runs, keeping the marker-id hygiene and banned-construct sweeps running against the regenerated text and the UNCONFIGURED-without-rjq behaviour unchanged.

§ 9.3
`W27` — --check compares two fresh temp regenerations for determinism instead of diffing against a committed file, so the mode keeps working with nothing tracked.

§ 9.4
`W28` — Flip the two PORTABILITY.md rows from running ./generate-portability.sh --check against the tree to running the portability contract test, whose temp regeneration carries the same drift detection.

§ 9.5
`W29` — Reword the loading-skills pointer from read-PORTABILITY.md to generate-then-read, and drop the stale hand-edit warning now that there is nothing committed to hand-edit.

§ 9.6
`W30` — Update references so the catalogue is described as generated on demand from portability-rules.json, keeping it the contract for the scripts.

## Testing requirement

| Test required | Rationale |
|---|---|
| yes | W26 is a test unit (temp-regeneration freshness arm with fault-injected marker drift) and W27 changes the generator's check mode with its own criteria. |

## Goal-size exception
