# Verification: 03-step-verify-capsule

## Automated tests

§ 2.1
Create a temp tag at HEAD, run benchmark/planning/setup-benchmark.sh against it, and assert the capsule contains five built libs and a REVIEWER.md matching the capsule SKILL.md hash; repeat once from the live tree.