# Verification: 07-step-install-skip-missing-binary

## Automated tests

§ 2.1
Scratch install without the binary exits 0 printing the notice once; with the binary the installed file set is identical to today; installer/build.sh --check passes.