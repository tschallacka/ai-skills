
## Cycle 1

| ID | Missing or over-broad item | Required plan change | Status | Work unit |
|---|---|---|---|---|
| AR-01 | No finding recorded yet. | N/A | ✅ resolved | N/A |
| AR-02 | **Generated-file targeted directly — edits are lost.** install.sh line 2-4 is `# GENERATED FILE — do not edit. Assembled from installer/src/*.sh by: installer/build.sh`. The plan (W21, goal:05 §6.1, W36/§6 lists README/install.sh as a plain file) directs editing `install.sh` directly. The authoritative inputs are `installer/src/05-config.sh` (SKILL_NAMES + SKILL_DESCRIPTIONS) and `installer/src/50-manifest.sh` (skill_files() chat block + requires group tables), regenerated via `installer/build.sh`. editing install.sh is overwritten on the next `build.sh` and breaks `planning/tests/test-installer-build.sh` (runs `build.sh --check`, exit 1 on stale). | Change every install.sh edit to target `installer/src/05-config.sh` + `installer/src/50-manifest.sh`, then run `installer/build.sh` to regenerate install.sh; add a work unit/step for the regeneration and the freshness check. | ❌ open | W21, W36 (goal:05) |
| AR-03 | **Rust crates are never shipped or built by the installer — rust-only skill is empty.** The plan's core promise (goal:03 §3.1, goal:05 §3.1, W21) is that the skill "ships only the rust server + rust client, with the binary built by the installer, not committed." That is unimplementable as written: (a) `src/chat-server-rs` and the planned `src/chat-client-rs` live OUTSIDE the `chat/` skill dir; `skill_files()` names `chat/`-relative paths and `source_file()` resolves `$SOURCE_ROOT/<skill>/<relative>` → `$SOURCE_ROOT/chat/...`, so it can never name `src/chat-server-rs` or `src/chat-client-rs`. (b) install.sh has NO cargo/build step at all (grep for cargo/build/target in install.sh returns only the `memlimit` usage hint at lines 679-680); the "binary built by the installer" has no mechanism. (c) None of build-release.sh's `collect()` paths include `src/` (it git-ls-files only planning/project-specificies/resource-limited-testing/brainstorm/post-implementation-review/todo/bug-report + listed_by_installer + install.sh/README/LICENSE/package.json), and package.json `files` lists `chat` but NOT `src`. So neither the existing server crate nor the new client crate is in the release tarball or npm package. Result: after removing the bash/interpreter tiers the published/installed skill contains no server and no client at all. | Decide and wire the real shipping/build path: either vendor the crate sources under `chat/`, or add `src/chat-server-rs` + `src/chat-client-rs` to `build-release.sh` collect() + package.json files, and add a real cargo build/install step to install.sh (or document prebuilt binaries). Add work units for the crate shipping + installer build step; remove the "built by the installer" claim until such a step exists. | ❌ open | W21, W37, W38, W34 (goal:05/03) |
| AR-04 | **Self-signed certificate generation needs a crate that is not available offline.** W06 requires the server to "generate a self-signed certificate at first run"; W01's dependency list is only rustls 0.23, rustls-pki-types, webpki-roots 0.26, ring. rustls does NOT generate certificates; you need a cert-minting crate (rcgen/yasna or similar). The cargo registry cache contains rustls/ring/webpki-roots/aws-lc but NO rcgen, yasna, or x509 cert-signing crate (verified: `ls ~/.cargo/registry/cache/*` | grep -iE 'rcgen|yasna|x509' → none). So under the plan's stated offline/`nix develop` resolution, W06 cannot generate a cert. (webpki-roots is also semantically wrong here: it is the public-CA trust store, unneeded for a self-signed/TOFU server.) | Add a cert-generation dependency (rcgen + its transitive deps), or define an explicit cert generation approach that resolves offline, and confirm it is cached; drop webpki-roots (or justify it). Fold the dependency into W01 and the cargo-cache verification. | ❌ open | W01, W06 |
| AR-05 | **The acceptance proof (real standard-client interop) has no concrete test artifact and the only TLS-capable executor is missing.** Goal:01 DoD and §8.1 make the third-party-standard-client connect the acceptance proof; the request requires it. W08 is the verification unit but its File is `N/A` and no goal-01 or goal-05 objective creates a script that drives a standard client over TLS. W38 (the rewritten chat/tests/test-chat.sh) explicitly says it drives "only the rust server and rust client" and "drops all runtime/interpreter/bash expectations" — it contains NO standard-client byte-sequence assertion. Additionally, `irssi`, `weechat`, `hexchat` are all MISSING on this host, so a real standard client is unavailable; the only TLS-capable tool present is `openssl` (`openssl s_client` can work) and `openssl` is never named in the plan. Without a named fixture driver and a concrete test file, the primary acceptance proof is un-wired. | Add a real standard-client verification script (e.g. a `chat/tests/standard-client.bats`/bash fixture using `openssl s_client` with a hand-built byte sequence asserting 001-005, 375/372/376, 353/366, and `:nick!user@host PRIVMSG #chan :text`), give W08 a file, and wire it into W38/run-tests. Name the driver. | ❌ open | W08, W38 |
| AR-06 | **The standard client's unavoidable CAP/caps handshake is undefined — the selected fixture would mask a real-client failure.** Every real IRCv2 client the plan names (irssi/WeeChat/HexChat) sends `CAP LS 302` then `CAP REQ`/`CAP END` immediately after connecting, before/around NICK+USER, and also expects a valid 005 with NICKLEN/CHANNELLEN/TARGMAX and a self-echo `JOIN` + (no) MODE/TOPIC on channel join. goal:01 §5.1 explicitly EXCLUDES IRCv3 caps/SASL, and goal:01 §5.1/goal:03 exclude MODE/op-management. The current handler replies `ERR unknown verb CAP` (main.rs:254), but the plan never specifies how the server responds to a `CAP` command from a real client. Because the plan's verification fixture is "our own faithful byte sequence," it would NOT send `CAP LS`, so it cannot detect that a real client stalls or aborts on an undefined CAP/005 response. This directly undermines the primary design driver. | Specify a minimal, real-client-compatible registration path: respond to `CAP LS`/`CAP END` (even if just `CAP * LS` + accept/END), emit a well-formed 005 with the required ISUPPORT tokens, and self-echo a correct `JOIN`; decide MODE/TOPIC-as-no-op if op features are excluded. Extend W03/W04 and make the standard-client fixture include the `CAP LS`/`CAP END` exchange. | ❌ open | W03, W04, W08 |
| AR-07 | **`id > since` vs the existing server's `id >= since` is unresolved — off-by-one risk.** The plan (W05, W16, goal:01 §3.1, §10.1) specifies FETCH returns "messages with id > since." But the existing rust server's `Hub::fetch` uses `id >= since` (main.rs:115), the bash local path uses `awk ... > s` (chat-read.sh:92), and chat-watch.sh:63 comments "FETCH semantics are >= since-id (python tier)". The tiers are already inconsistent and the plan does not state which bound the new server/client will use, nor update the `terminating marker` (W05 "followed by a terminating marker" never gives the marker command/syntax). An implementer picking `>=` would make the client re-deliver the last line (chat-watch.sh:63-74 documents exactly this trap). | Pin the FETCH bound explicitly (`>` since, matching the plan text and chat-read.sh) and reconcile/transport it into the shared protocol module (W02); define the terminating marker byte-sequence (e.g. `:server 997 EOF` or a documented numeric) so server and client agree; update the now-removed chat-watch.sh comment references. | ❌ open | W02, W05, W16 |
| AR-08 | **cargo/rustc are not on PATH — run-tests.sh cannot build the binarmies the rewritten test drives.** `cargo` and `rustc` are MISSING from the shell (`command -v cargo` → none); they only exist inside `nix develop` (flake.nix:125-135). `run-tests.sh` executes `chat/tests/test-chat.sh` directly WITHOUT entering `nix develop`. W38 says the rewritten "bash test that drives the two binaries" must "start the rust server, run the rust client" — but nothing builds them. Today test-chat.sh SKIP-louds when the binary is absent (lines 201-202); after the rewrite the test will fail to even locate a server binary. The plan never adds a build step (nix develop --command cargo) to W38 or the consumer/verification steps. | Add an explicit build step (e.g. `nix develop --command cargo build --release --manifest-path src/chat-server-rs/Cargo.toml` and the client crate) to the test/verification path, or state that binaries must be prebuilt before the suite; make W38 and W15/W09 depend on it. | ❌ open | W38, W15, W08 |
| AR-09 | **rustls default provider is aws-lc-rs; the plan's "ring backend" is not actually encoded.** §10.1(c) and W01 "prefer ring, confirm aws-lc-rs is not forced," but W01's Cargo.toml change text just lists `rustls 0.23 ... ring` with no feature selection. rustls 0.23.43 defaults to the `aws-lc-rs` provider (aws-lc-sys is in the cache, needs cc/cmake/C toolchain — violating the zero-C-toolchain goal and §9 musl preference). Without `default-features = false, features = ["ring", "std", "tls12"]`, cargo pulls aws-lc-rs. | Specify the exact rustls feature set that selects the ring provider in W01/W10's Cargo.toml change, and verify with a lockfile/cache check that aws-lc-rs is not in the resolved tree. | ❌ open | W01, W10 |
| AR-08b | **PORTABILITY.md/Portability-contract expectations and the removed python3 group are unplanned.** Requires.tsv W33 removes the python3/node/perl/socat group, and the plan deletes the UDP scripts + chat-server.sh. But `planning/tests/test-portability-contract.sh` still allowlists `./chat/scripts/chat-announce.sh:python3-shipped`, `chat-discover.sh:python3-shipped`, `chat-server.sh:python3-shipped`, `chat/tests/test-chat.sh:python3-shipped`, and `./install.sh:python3-shipped` (lines 58-59, 71-72, 85); deleting those files while the allowlist names them, or editing requires.tsv without regenerating PORTABILITY.md (`./generate-portability.sh`), trips the freshness check and the portability scan. The plan lists requires.tsv + tests but not PORTABILITY.md regeneration/npm-ignore regeneration or the portability allowlist edits. | Add a step to update `planning/tests/test-portability-contract.sh` allowlist rows and to run `./generate-portability.sh` (and `installer/build-release.sh --npmignore`) after the removals, and record PORTABILITY.md in the affected consumer files. | ❌ open | W33, W27, W20 (goal:03/05) |
