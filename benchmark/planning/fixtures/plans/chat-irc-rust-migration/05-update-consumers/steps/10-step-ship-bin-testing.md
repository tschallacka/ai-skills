# Verification: 10-step-ship-bin

## Automated tests

§ 2.1
installer/build.sh regenerates install.sh; the chat block lists chat/bin/ binaries; the built binaries exist under chat/bin after the cargo step; npm pack dry-run includes chat/bin/*.