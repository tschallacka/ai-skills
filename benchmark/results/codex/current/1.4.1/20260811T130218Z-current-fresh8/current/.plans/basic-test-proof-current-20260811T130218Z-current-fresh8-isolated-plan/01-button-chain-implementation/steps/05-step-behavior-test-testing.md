# Verification: 05-step-behavior-test

## Automated tests

Run `node button-chain.behavior.test.mjs` after future implementation. The command must use only built-in Node.js modules and must fail unless initial state, append behavior, non-last protection, fourth-generated completion, exact text, visible white border, and border contrast all pass.
