# Testing companion: 01-step-initial-markup

## Browser verification

Future execution only. Open `button-chain.html` after implementation and confirm the initial view contains exactly one visible button inside `#button-chain-root`, with no generated buttons and no `finished` text.

## Acceptance coverage

Covered downstream by W06. The browser story must fail if the initial button count is not exactly one.
