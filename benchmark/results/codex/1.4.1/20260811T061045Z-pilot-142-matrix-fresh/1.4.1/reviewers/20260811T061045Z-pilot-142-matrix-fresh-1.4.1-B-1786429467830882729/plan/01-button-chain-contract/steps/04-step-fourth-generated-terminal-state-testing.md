# Testing Companion: 04-step-fourth-generated-terminal-state

## Automated tests

Future execution should assert that `renderFinishedState()` clears prior document content and renders the exact lowercase text `finished` when called by the terminal handler branch.

## Browser verification

In the future browser run, terminal behavior is reached through W07. Pass the rendering check only if all previous buttons disappear and the page displays the exact lowercase text `finished` with the planned completion styling. This benchmark did not open or test HTML.
