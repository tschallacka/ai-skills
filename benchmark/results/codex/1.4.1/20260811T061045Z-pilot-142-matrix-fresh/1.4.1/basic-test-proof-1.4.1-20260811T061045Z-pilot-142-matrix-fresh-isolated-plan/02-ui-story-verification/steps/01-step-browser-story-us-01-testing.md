# Testing Companion: 01-step-browser-story-us-01

## Browser verification

Execute `US-01` from `ui-user-stories.md` using the cached sequence in `ui-story-runs/US-01.md`. Use only direct browser clicks on the visible current last button. Pass only when clicks one through four each append exactly one button below the previous last button, and click five on generated button 4 clears the document and shows `finished` with a visible white border.

## Automated tests

No automated test replaces the UI story. Automated assertions may support the future run, but `US-01` remains unpassed until browser evidence is recorded.
