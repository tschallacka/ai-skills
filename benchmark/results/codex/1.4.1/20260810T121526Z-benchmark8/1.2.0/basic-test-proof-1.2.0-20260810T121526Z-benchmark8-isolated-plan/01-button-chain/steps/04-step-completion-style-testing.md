# Testing companion: completion style

After the future terminal click, inspect the rendered completion state visually. Pass only if the border is visibly white and surrounds the exact lowercase `finished`. Not executed in this planning-only proof.
