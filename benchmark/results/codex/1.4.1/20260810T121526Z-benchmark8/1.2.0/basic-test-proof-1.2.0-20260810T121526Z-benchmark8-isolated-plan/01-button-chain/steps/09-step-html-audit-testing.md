# Testing companion: HTML artifact audit

Use a read-only filename audit limited to the isolated benchmark workspace. Pass only with zero HTML/HTM artifacts; do not inspect the repository or create a test HTML file.
