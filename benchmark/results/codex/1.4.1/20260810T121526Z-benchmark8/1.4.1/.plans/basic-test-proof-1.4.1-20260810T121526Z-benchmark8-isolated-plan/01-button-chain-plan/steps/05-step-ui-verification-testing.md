# Testing companion: 05-step-ui-verification

## Browser verification

Run US-01 as one bounded direct-input flow using its cache. Navigate to the local file, perform the recorded mouse clicks in order, wait only for the rendered count or terminal completion element, and record pass/fail evidence. This planning-only proof must leave the story explicitly excluded and must not start a browser, server, driver, or HTML execution.

## Evidence

Current result: excluded from execution by the user’s explicit no-browser/no-HTML instruction. The cache records the future actions and readiness checks; no pass is claimed.
