# Testing companion: 01-step-initial-markup

## Browser verification

Future run: open the local `button-chain.html` route in a fresh browser context and inspect the rendered page. Pass when exactly one visible button is present inside the named container and no completion text is present. This benchmark does not execute the browser flow by explicit user instruction.

## Evidence

Planned evidence is the initial screenshot or rendered assertion and the observed button count. Current result: not run; HTML was not created or opened.
