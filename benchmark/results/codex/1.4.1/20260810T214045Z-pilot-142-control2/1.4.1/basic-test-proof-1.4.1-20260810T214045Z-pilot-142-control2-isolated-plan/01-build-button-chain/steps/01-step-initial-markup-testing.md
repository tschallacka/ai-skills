# Testing companion: 01-step-initial-markup

## Static review

- After future implementation, inspect `button-chain.html`.
- Pass when the document has a single visible initial button inside `#button-chain-root`.
- Fail if there is more than one initial button, a pre-rendered generated button, pre-rendered `finished` text, or a second implementation file.

## Planning-proof status

Not executed in this benchmark run. The proof is planning-only and must not create or inspect HTML.
