# Testing Companion: 01-step-initial-button-markup

## Automated tests

Future execution should include a bounded assertion for the initial rendered state of `button-chain.html`: the document contains `#button-chain-root` and exactly one visible `button` before any click occurs.

## Browser verification

Open the future `button-chain.html` file in a browser after implementation. Pass only if the initial page shows exactly one button and no pre-rendered generated buttons or `finished` message. This benchmark did not open or test HTML.
