# Testing companion: 02-step-append-handler

## Browser verification

Future execution only. Click the current last button and verify exactly one new button appears below it. After a second button exists, click the earlier button and verify no new button is appended.

## Acceptance coverage

Covered downstream by W06. The browser story must assert one append per accepted current-last click and no unexpected extra append.
