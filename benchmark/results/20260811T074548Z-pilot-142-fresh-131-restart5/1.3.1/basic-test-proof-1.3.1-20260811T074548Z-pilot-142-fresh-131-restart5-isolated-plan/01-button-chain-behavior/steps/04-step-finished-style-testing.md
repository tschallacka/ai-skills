# Testing companion: 04-step-finished-style

## Browser verification

Future execution only. After completion, inspect the rendered result visually and through computed style when available. The text must be exactly `finished` and the border must be visible and white.

## Acceptance coverage

Covered downstream by W06. The browser story should check exact text and visible white border on `.completion-message`.
