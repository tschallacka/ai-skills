# Testing companion: initial markup

Verify the future file's initial render in a browser: open the local file and observe one visible button in `#button-chain`. Pass only when no second button exists before interaction and the button is usable by mouse click. This companion is not executed in the current proof.
