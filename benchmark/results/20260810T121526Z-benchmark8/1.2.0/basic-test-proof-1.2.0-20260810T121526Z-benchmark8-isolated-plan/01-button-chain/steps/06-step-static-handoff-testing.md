# Testing companion: static handoff

Run the exact tagged `planning/scripts/validate-plan.sh` command against the plan and require exit code zero. Record only the command, exit code, and complete validator output for W07; W09 and W10 separately own artifact and process audits.
