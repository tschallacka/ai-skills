# Testing companion: completion behavior

In the future browser run, click `Button 0`, `Button 1`, `Button 2`, and `Button 3` to create generated buttons 1–4, then explicitly click terminal generated `Button 4`. Pass only if that final click clears prior content and leaves exact lowercase `finished`, with no chain button remaining. Not executed in this proof because HTML testing is forbidden.
