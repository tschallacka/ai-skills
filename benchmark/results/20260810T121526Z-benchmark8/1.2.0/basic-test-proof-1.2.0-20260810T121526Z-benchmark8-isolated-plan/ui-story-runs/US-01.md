# Browser run cache: US-01

## Starting state

- Future URL: local `button-chain.html`; visitor persona; default desktop viewport; one initial visible button in the document.

## Buffered interaction sequence

| Order | Direct UI input | Target / value | Expected readiness signal |
|---|---|---|---|
| 1 | Mouse click | Accessible button `Button 0` | Exactly one generated button, `Button 1`, is visible below it. |
| 2 | Mouse click | Accessible button `Button 1` | Exactly one new button, `Button 2`, is visible below it. |
| 3 | Mouse click | Accessible button `Button 2` | Exactly one new button, `Button 3`, is visible below it. |
| 4 | Mouse click | Accessible button `Button 3` | Exactly one new button, `Button 4`, is visible below it. |
| 5 | Mouse click | Accessible button `Button 4` | The document is cleared and exact text `finished` with a visible white border is visible. |

## Waits and readiness

| After order | Wait or condition | Maximum wait | Observed result |
|---|---|---|---|
| 1–4 | Wait until the named next button is visibly rendered and available | 2 s each | Not observed; browser run prohibited in this proof. |
| 5 | Wait until the completion text and white border are visibly rendered | 2 s | Not observed; browser run prohibited in this proof. |

## Run result

- Status: `💤 untested`
- Evidence: No screenshot or URL observation was collected because HTML creation and browser execution are prohibited by the benchmark safety boundary. This cache is the future executor's exact click sequence.
- Cache validity: Valid for the specified contract; invalidate if the future implementation changes the initial target, button-count semantics, route, or completion presentation.
