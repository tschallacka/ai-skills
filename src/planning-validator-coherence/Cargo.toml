# MODE: DEV
# PACKAGE: PROD
[package]
name = "planning-validator-coherence"
version = "0.1.0"
edition = "2021"
description = "Intra-document self-coherence validation for planning documents"
license = "MIT"

[lib]
name = "planning_validator_coherence"
path = "src/lib.rs"

[dependencies]
planning-validator-common = { path = "../planning-validator-common" }
planning-validator-stale = { path = "../planning-validator-stale" }
regex = { workspace = true }
