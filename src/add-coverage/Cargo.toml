# MODE: DEV
# PACKAGE: PROD
[package]
name = "add-coverage"
version = "0.1.0"
edition = "2021"
description = "Add planning definition-of-done coverage"
license = "MIT"

[dependencies]
planning-core = { path = "../planning-core" }

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
