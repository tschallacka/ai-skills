# MODE: DEV
# PACKAGE: PROD
[package]
name = "generate-portability"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
