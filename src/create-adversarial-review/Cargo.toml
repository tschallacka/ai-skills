# MODE: DEV
# PACKAGE: PROD
[package]
name = "create-adversarial-review"
version = "0.1.0"
edition = "2021"
description = "Create a planning adversarial review artifact"
license = "MIT"

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
