# MODE: DEV
# PACKAGE: PROD
[package]
name = "add-ui-story-links"
version = "0.1.0"
edition = "2021"
description = "Update planning UI story work-unit links"
license = "MIT"

[dependencies]
planning-core = { path = "../planning-core" }
