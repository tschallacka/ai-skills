# MODE: DEV
[toolchain]
# The same pin src/chat-server-rs and src/tony-the-pony carry: one compiler for
# the repository, so a lint never fails on a contributor's newer toolchain alone.
channel = "1.98"
components = ["rustfmt", "clippy"]
profile = "minimal"
