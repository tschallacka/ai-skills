# MODE: DEV
# PACKAGE: PROD
[package]
name = "todo-update"
version = "0.1.0"
edition = "2021"

[dependencies]
planning-register = { path = "../planning-register" }
serde_json = { version = "1", features = ["preserve_order"] }
