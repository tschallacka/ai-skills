# MODE: DEV
# PACKAGE: PROD
[package]
name = "planning-table"
version = "0.1.0"
edition = "2021"
description = "Markdown and CSV table logic shared by planning Rust helpers"
license = "MIT"

[lib]
name = "planning_table"
