# MODE: DEV
# PACKAGE: PROD
[package]
name = "create-progress"
version = "0.1.0"
edition = "2021"
description = "Create a planning goal progress tracker"
license = "MIT"

[dependencies]
planning-core = { path = "../planning-core" }
planning-progress = { path = "../planning-progress" }
