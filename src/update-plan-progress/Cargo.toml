# MODE: DEV
# PACKAGE: PROD
[package]
name = "update-plan-progress"
version = "0.1.0"
edition = "2021"
description = "Update a planning plan-level progress tracker"
license = "MIT"

[dependencies]
planning-core = { path = "../planning-core" }
planning-progress = { path = "../planning-progress" }
