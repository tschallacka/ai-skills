# MODE: DEV
# PACKAGE: PROD
[package]
name = "register-command"
version = "0.1.0"
edition = "2021"
description = "Maintain a plan command registry"
license = "MIT"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
