# MODE: DEV
# PACKAGE: PROD
[package]
name = "register-rebuild"
version = "0.1.0"
edition = "2021"
description = "Repair and validate planning JSON registers"
license = "MIT"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
