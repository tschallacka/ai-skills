# MODE: DEV
# PACKAGE: PROD
[package]
name = "planning-register"
version = "0.1.0"
edition = "2021"
description = "Shared JSON register operations for planning tools"
license = "MIT"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
