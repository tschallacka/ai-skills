# MODE: DEV
# PACKAGE: PROD
[package]
name = "planning-validator-comparisons"
version = "0.1.0"
edition = "2021"
description = "Artifact-comparison validation for planning documents"
license = "MIT"

[lib]
name = "planning_validator_comparisons"
path = "src/lib.rs"

[dependencies]
planning-validator-common = { path = "../planning-validator-common" }
serde_json = { version = "1", features = ["preserve_order"] }
