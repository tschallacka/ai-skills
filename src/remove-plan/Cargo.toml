# MODE: DEV
# PACKAGE: PROD
[package]
name = "remove-plan"
version = "0.1.0"
edition = "2021"
description = "Remove a planning plan"
license = "MIT"

[dependencies]
planning-core = { path = "../planning-core" }
