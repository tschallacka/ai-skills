# MODE: DEV
# PACKAGE: PROD
[package]
name = "register-read"
version = "0.1.0"
edition = "2021"
description = "Read-only defect and TODO register queries"
license = "MIT"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
