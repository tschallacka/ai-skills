# Testing: 01-step-markup

## Automated tests

- Future check only: inspect `button-chain.html` without changing it and assert exactly one initial button exists in `#button-chain`, no generated button exists initially, and the chain layout places later children on separate rows.
- Pass only if the completion presentation declares a white border and no executable append behavior is owned by W01.
- Do not run this check during the planning-only proof.

## Browser verification

- Covered downstream by W03 / US-01 after W02 and W07 pass.
- Do not open or render HTML during the planning-only proof.
