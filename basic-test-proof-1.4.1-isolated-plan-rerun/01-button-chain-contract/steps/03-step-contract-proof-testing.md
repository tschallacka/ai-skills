# Testing: 03-step-contract-proof

## Manual verification

- Future semantic review only: record pass/fail for initial count 0; four one-button appends; current-last-only authority; vertical placement; generated-button-4 completion; no fifth generated button; whole-document clearing; exact `finished`; and visible white border.
- W07 passes only if every invariant passes. It must not alter implementation.
- Do not inspect HTML during the planning-only proof.
