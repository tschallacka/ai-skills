# Verification

Run `planning/tests/test-plan-env.sh` under an isolated temporary plans root. Require explicit pass/fail output for creation, refresh, permissions, quoting, isolation, safe sourcing, malformed input, root containment, and cleanup.
