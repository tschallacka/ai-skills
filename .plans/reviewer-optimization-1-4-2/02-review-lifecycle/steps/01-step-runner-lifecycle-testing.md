# Verification

Run the bounded runner fixture with iterative mode, a forced reviewer limit, and an interrupt case.

Pass when lifecycle events record reviewer session, cycle, finding owner, verification pass, closure, handoff, and termination, and no descendant survives.

