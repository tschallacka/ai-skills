# Verification

Exercise invalid options, fresh-review default, pass-limit termination, fresh-review replacement, and lifecycle metadata using the runner fixture.

Pass when invalid inputs fail before workers start and bounded runs produce the expected event records.

