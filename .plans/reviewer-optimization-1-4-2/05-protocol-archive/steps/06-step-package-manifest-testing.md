# Verification

Run the manifest emitter/validator against the changed package file set.

Pass when every installable file has exactly one manifest entry and source-only brainstorm inputs are excluded from installation.

