# Verification

Run the analyzer against fixtures containing legacy and 1.4.2 results, missing telemetry, and ambiguous counts.

Pass when cohort summaries remain separate, within-cohort comparisons are authoritative, cross-cohort comparisons are contextual, and unknown counts say `not recorded`.

