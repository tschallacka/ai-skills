# Verification

Run batch setup with a legacy tag and a 1.4.2 tag, then inspect run metadata and unchanged legacy directories.

Pass when the new protocol identifier, reviewer mode, access mode, and cohort are recorded and legacy artifacts are not rewritten.

