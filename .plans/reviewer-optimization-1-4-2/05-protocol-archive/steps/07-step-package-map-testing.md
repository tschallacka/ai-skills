# Verification

Validate the package map against the manifest and installer destination rules.

Pass when all six columns are populated, every manifest file has one destination owner, and no destination collision is introduced.

