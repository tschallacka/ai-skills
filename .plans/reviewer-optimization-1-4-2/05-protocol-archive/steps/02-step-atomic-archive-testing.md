# Verification

Inject worker, telemetry, and analyzer failures during staged archive creation.

Pass when incomplete runs are not published, successful runs are self-describing, and every published archive contains tagged skill provenance.

