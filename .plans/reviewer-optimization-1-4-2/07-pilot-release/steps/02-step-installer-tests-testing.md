# Verification

Run `planning/tests/test-installer-manifest.sh` and the installer dry-run/collision checks.

Pass when exact package coverage, approval refusal, collision refusal, backup behavior, and no-partial-install guarantees hold.

