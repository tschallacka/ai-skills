# Verification

Run the exact documented matrix with fixed tags, task inputs, isolated roots, and protocol metadata. Pass only when required telemetry, final independent review, defect detection, validation, and token/latency thresholds are met; otherwise fail closed.

