# Verification

Run `planning/tests/test-planning-context-v27-contract.sh` against the updated matrix, oracle, and benchmark fixtures.

Pass when authority/recovery, context namespace, checkpoint, retry, and compact-read expectations match without mutation regressions.

