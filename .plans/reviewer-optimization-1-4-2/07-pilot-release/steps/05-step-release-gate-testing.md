# Verification

Run all focused shell tests, package checks, benchmark context audits, and the final protocol validation command.

Pass when every required artifact, manifest entry, contract test, and release gate succeeds with no unresolved safety or independence finding.

