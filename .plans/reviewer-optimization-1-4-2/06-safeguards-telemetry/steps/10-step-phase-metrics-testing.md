# Verification

Compare phase metrics in raw telemetry with runner timestamps, event counts, command logs, and artifact sizes. Pass when exact values are marked exact, inferred values heuristic, and unavailable values are not reported as zero.

