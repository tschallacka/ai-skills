# Verification

Run telemetry lookup fixtures with exact match, stale match, ambiguous database, missing database, and rollout fallback cases.

Pass when only the exact worker UUID is selected, ambiguity taints the result, and path/lookup method are recorded.

