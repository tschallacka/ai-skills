# Verification

Inject one failure at a time in access, process, telemetry, worker, reviewer, analyzer, and validation layers.

Pass when evaluation evidence preserves the raw cause and does not relabel one layer as another.

