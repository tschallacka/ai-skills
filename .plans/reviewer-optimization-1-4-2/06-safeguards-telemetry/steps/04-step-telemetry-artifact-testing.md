# Verification

Validate the emitted raw telemetry JSON against its schema using worker and reviewer fixture records.

Pass when IDs, phase boundaries, token/cache fields, counts, durations, tool volumes, and parent/child lifecycle records retain provenance and exact/heuristic classification.

