# Verification

Interrupt a run with active worker, reviewer, analyzer, and child processes, then inspect process-group and temporary-state audits.

Pass when the command returns the interrupted status and no owned descendant survives.

