# Verification

Execute the access-control fixture for allowed files, blocked source, blocked prior results, and escape attempts.

Pass when each case produces the documented availability and taint result without reading outside its capsule.

