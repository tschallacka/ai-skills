# Verification

Run the worker boundary fixture and command/path audit.

Pass when an allowed read succeeds, an unallowlisted repository/installed-skill/previous-result read is unavailable or taints the run, and per-worker variables cannot collide.

