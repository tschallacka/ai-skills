# Verification

Inspect a generated case capsule and compare its file list with the explicit allowlist.

Pass when it contains only task specification, tagged skill/profile, resolved references, and required runner inputs, with workspace and archive roots separate.

