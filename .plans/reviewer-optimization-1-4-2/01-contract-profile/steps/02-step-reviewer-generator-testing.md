# Verification

Run `planning/scripts/generate-reviewer.sh` against a temporary output and compare its section allowlist, version, and source hash with the expected contract.

Pass when missing or empty required sections fail closed and valid output is deterministic.

