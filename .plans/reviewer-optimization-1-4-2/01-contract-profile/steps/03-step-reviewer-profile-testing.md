# Verification

Regenerate `planning/REVIEWER.md` in a temporary copy and compare it byte-for-byte with the committed profile.

Pass when the generated profile contains the source hash/version and no hand-edited content is required.

