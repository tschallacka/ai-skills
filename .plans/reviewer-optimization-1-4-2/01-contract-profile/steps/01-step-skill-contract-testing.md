# Verification

Run the planning helper contract tests and inspect the marked reviewer sections.

Pass when the contract names iterative mode, hard limits, contamination rules, capsule boundary, protocol metadata, and telemetry fields without contradicting the fresh-review default.

