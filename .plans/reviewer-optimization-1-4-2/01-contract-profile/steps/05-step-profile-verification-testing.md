# Verification

Run the generator and focused tests in a clean temporary skill copy, then compare the generated profile and recorded hash with the repository artifacts.

Pass when all commands exit zero and the committed output is reproducible.

