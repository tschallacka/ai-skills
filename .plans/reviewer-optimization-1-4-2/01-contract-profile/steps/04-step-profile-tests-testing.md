# Verification

Run the focused profile and plan-command tests in `planning/tests/test-plan-commands.sh`.

Pass when valid marked sections succeed, missing/empty sections fail, and source/profile drift is detected.

