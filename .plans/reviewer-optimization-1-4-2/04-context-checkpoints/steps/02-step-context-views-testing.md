# Verification

Exercise each fixed plan-context view with bounded byte/record limits.

Pass when summaries, ownership/dependency, changed-document, and validator-focused reads stay within limits and preserve the command contract.

