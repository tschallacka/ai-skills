# Verification

Run `planning/tests/test-plan-context.sh` cases that initialize and refresh a source-aware context index.

Pass when SKILL.md, REVIEWER.md, approved references, and plan files receive hashes and stale entries are invalidated after mutation.

