# Verification

Run the complete bounded context test suite, including mutation, changed-entry, invalidation, and concurrent-worker cases.

Pass when all expected cache states and no-mutation guarantees are observed.

