# Testing: Goal 02 / Step 01

## Automated tests

Extend `test-review-lifecycle.sh` and `test-safeguards.sh` with true approval,
false approval, missing approval, duplicate approval locations, and conflicting
approval cases. Assert distinct review-completed, plan-approved, and adoptable
fields and fail-closed reasons.
