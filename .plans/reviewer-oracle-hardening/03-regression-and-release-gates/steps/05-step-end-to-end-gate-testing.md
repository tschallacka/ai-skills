# Testing: Goal 03 / Step 05

## Automated and integration verification

Run the complete relevant contract suite under the resource-limited wrapper,
then run the current-only benchmark and inspect its semantic oracle, approval,
telemetry, lifecycle, evaluation, and comparison artifacts. Finish with shell
syntax, JSON, plan validation, and `git diff --check`.
