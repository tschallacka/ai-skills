{
  "skill": "bug-report",
  "skill_version": "1.4.2",
  "comment": "Defects found in this repository's own skills and installer.",
  "bugs": [
    {
      "id": "B2",
      "title": "The shipped core library lost plan_stat_mode and plan_stat_uid, so an installed skill could not read a file mode",
      "status": "fixed",
      "severity": "blocking",
      "priority": "urgent",
      "parent": null,
      "reproduce": "bash planning/scripts/build-plan-libs.sh; grep -c \"^plan_stat_mode()\" planning/scripts/plan-core-lib.sh",
      "observed": "0 definitions. Two tests said so: test-portable-helpers.sh reported plan_stat_mode: command not found for three assertions, and test-duplication-ratchet.sh reported it is referenced by MAINTAINER.md section 3 but no longer exists.",
      "expected": "Both helpers present in the compiled library; plan_atomic_write calls plan_stat_mode to preserve a file mode.",
      "mechanism": "The re-marking script kept PACKAGE: DEV on any path matching *probe* -- intended for a planted test fixture named plan_probe_dev_only.sh -- and planning/scripts/lib/core/plan_stat_probe.sh matched on its name. PACKAGE: DEV means dev-build-only, so the prod build excluded it.",
      "surfaces": [
        "planning/scripts/lib/core/plan_stat_probe.sh",
        "planning/scripts/plan-core-lib.sh"
      ],
      "fix": "Marked plan_stat_probe.sh PACKAGE: PROD and rebuilt. Every other compiler input was checked for the same mistake; it was the only one.",
      "verification": "test-portable-helpers.sh and test-duplication-ratchet.sh both pass; five stat-helper definitions and calls are back in plan-core-lib.sh. Re-marking the file PACKAGE: DEV reproduces the failure.",
      "found_by": "the both-shell run of the release commit, on both bash 5.3 and 3.2",
      "notes": "A name-substring match standing in for a marker. The marker was right in every file; the script that wrote it guessed from the filename. It also shows the release checks were not sufficient on their own: build-release.sh --list and a by-hand diff of npm pack both passed a library with a function missing, because they compare file sets and not file contents.",
      "created_at": "2026-08-20T16:21:35Z",
      "updated_at": "2026-08-20T16:21:35Z"
    },
    {
      "id": "B31",
      "title": "test-plan-overview fails on both macOS legs and reports nothing, so the PR is red with no diagnosis",
      "status": "fixed",
      "severity": "blocking",
      "priority": "urgent",
      "parent": null,
      "reproduce": "CI run 32726302448 on PR #3 (head f699f84). Both macOS jobs: 96 ran, 95 passed, 1 failed -- test-plan-overview, exit 1. ubuntu-latest passes all 96, and ./verify-both-shells.sh passes 96/96 on bash 5.3 and bash 3.2 on Linux.",
      "observed": "The only output under the FAIL line is the limited-run wrapper's memlimit warnings. No assertion text, no t_end summary, and no sed/awk/grep error anywhere in the full job log.",
      "expected": "Either the test passes on macOS, or it says which assertion failed.",
      "mechanism": "Confirmed by the speaking trap on CI run 32751254496: test-plan-overview.sh line 90 called timeout(9) on render-plan-overview --watch, and macOS does not ship timeout (the B6 class). The watcher died instantly of exit 127, watch.log stayed empty, grep -c exited 1 under pipefail+set -e, and the ERR trap named the exact line — the diagnosis the first half of the fix was built to produce.",
      "surfaces": [
        "planning/tests/test-plan-overview.sh",
        "planning/scripts/render-plan-overview.sh",
        "planning/scripts/update-step.sh"
      ],
      "fix": "this session — the watch case bounds itself by polling for the second render (no timeout binary); chat/tests/test-chat.sh swaps its one timeout(5) for a read -t loop with the same ceiling; render-plan-overview traps TERM/INT so a stopped watcher exits promptly instead of deferring the signal behind its sleep child",
      "verification": "PR #3 run 32756400447: both macOS legs pass 97/97 with test-plan-overview green on macOS system bash 3.2 and the runner default bash — first macOS pass since the renderer landed. Linux unchanged.",
      "found_by": "CI on PR #3, opened for exactly this reason: 46 commits with 88 files changed had never run on BSD userland, and the branch itself had just made the macOS legs blocking",
      "notes": "Blocking because -ox removed continue-on-error from both macOS legs (T37), so this fails the PR rather than being informational. Diagnose it on a runner, not by reading: stop discarding the setup commands' output first -- capture and print it on failure -- and the cause will name itself. Worth checking the same pattern in the other new tests from this branch, since a setup command redirected to /dev/null hides a BSD failure the same way anywhere it appears. Order of work: make the test able to speak before fixing what it fails on -- capture the setup commands' output and print it on failure -- then push and read the macOS leg rather than reading the code. The same discarded-output pattern is worth checking in the other tests this branch added: test-add-work-unit-staging, test-adversarial-review-mint-order, test-adversarial-review-preamble, test-function-length-ratchet, test-installer-opencode-permissions, test-owned-roster-scaffold, test-verifier-reach-memo and chat/tests/test-chat.sh. And the flag cannot go back: making those legs blocking is what caught this.",
      "created_at": "2026-08-24T12:26:51Z",
      "updated_at": "2026-08-24T17:36:36Z"
    },
    {
      "id": "B33",
      "title": "Declaring chat's runtimes puts python3 into the generated install.sh, which the python3-shipped gate bans",
      "status": "fixed",
      "severity": "major",
      "priority": "urgent",
      "parent": null,
      "reproduce": "bash planning/tests/test-portability-contract.sh   # at fa1bd62",
      "observed": "portability: ./install.sh uses banned construct 'python3-shipped' at line 417 -- see PORTABILITY.md. install.sh:417 is the generated members table: @server-runtimes) case \"$platform\" in *:*) printf '%s\\n' python3 node perl socat ;; esac ;;",
      "expected": "Either declaring an optional runtime is allowed, or the gate says why it is not, but the suite is not red for doing the thing T39 asked for.",
      "mechanism": "e6492f6 declared the any-of group in chat/requires.tsv, and installer/build.sh generates the requirement tables into install.sh, so the literal python3 now appears in the shipped installer. The portability gate scans every tracked *.sh including the generated install.sh, and its allowlist exempts only ./chat/scripts/chat-server.sh and ./chat/tests/test-chat.sh. So the declaration the brief asked for is exactly what trips the rule.",
      "surfaces": [
        "planning/tests/test-portability-contract.sh",
        "portability-rules.json",
        "installer/build.sh",
        "chat/requires.tsv"
      ],
      "fix": "4befd4e — exact-file allowlist row ./install.sh:python3-shipped in test-portability-contract: the generated tables NAME optional runtimes to verify and hint them, which is not requiring one; strength still decides",
      "verification": "test-portability-contract passes on both legs; removing the row restores both findings. The rule-vs-gate question itself stays open as T40.",
      "found_by": "the 5-minute watch: CI run 32728982657 showed ubuntu failing two tests rather than the one already recorded, and it reproduces locally",
      "notes": "This is T40 arriving as a blocker rather than a discussion. Do not reach for an allowlist row on install.sh: that file is generated from every skill's requires.tsv, so exempting it turns the rule off for anything any skill ever declares. The rule text is what is wrong -- it is named python3-shipped and it means do not depend on python3, while what it detects is the word appearing in a shell file. Naming an optional runtime in a fallback chain is not depending on it. Options, cheapest first: narrow the detect so it matches an invocation rather than the bare word; or scope the rule to hard requirements and let a soft or grouped declaration through; or rescope it to shipped runtime code, which is the case it was written for and which no *.sh scan can see anyway (chat/runtime/server.py has always been invisible to it).",
      "created_at": "2026-08-24T12:57:23Z",
      "updated_at": "2026-08-24T14:45:05Z"
    },
    {
      "id": "B36",
      "title": "A goal with no bare work-unit ids in its roster aborts validate-plan, which exits 1 with no verdict",
      "status": "fixed",
      "severity": "major",
      "priority": "urgent",
      "parent": null,
      "reproduce": "cp -R .plans/harden-test-reporting-and-plan-dir /tmp/rost/plan; g=/tmp/rost/plan/01-plan-dir-synonym/goal.md; grep -v '^`W[0-9]' \"$g\" > \"$g.n\" && mv \"$g.n\" \"$g\"; bash planning/scripts/validate-plan.sh /tmp/rost/plan; echo \"exit $?\"",
      "observed": "exit 1 with four FAIL lines and no summary at all -- no \"Plan validation failed with N error(s).\" and no \"passed\" line. bash -x ends: printf '%s\\n%s\\n' '' '' / grep -E '^W[0-9][0-9]+$' / sort -u / tr, then roster_ids= followed immediately by plan_validate_cleanup, which is the EXIT trap rather than the next pass.",
      "expected": "A goal whose roster names no unit is a finding or a warning, and the run still reports its verdict.",
      "mechanism": "validate-plan-propagation-lib.sh:347 assigns from a pipeline: roster_ids=\"$(printf ... | grep -E '^W[0-9][0-9]+$' | sort -u | tr '\\n' ' ')\". With both awk passes empty the printf emits two blank lines, grep matches nothing and exits 1, pipefail makes that the pipeline's status, the assignment inherits it, and set -e aborts the script -- before the summary and before every later pass. The reporter saw the WARN-only variant, which is the same abort in a plan whose earlier passes had produced no FAIL lines; the missing summary is the signature either way.",
      "surfaces": [
        "planning/scripts/validate-plan-propagation-lib.sh"
      ],
      "fix": "this session — the roster_ids pipeline guards its grep with || true inside a brace group, the same escape every sibling assignment in the file already uses; an empty match is an answer, not an error",
      "verification": "New empty-roster case in test-roster-cross-reference.sh: stripping the bare WNN blurbs must still produce roster-omits findings and a Plan validation failed verdict. Mutation: reverting to the unguarded grep fails with \"the roster check never ran\" — exactly the missing-summary signature.",
      "found_by": "an active worker on the installed skill, hitting it on goal 05; reproduced here against the current tree",
      "notes": "Worse than a wrong verdict: the run reports none, and every pass after the roster is skipped silently, so a plan can be far more broken than the output suggests. A guard on that assignment is the immediate fix, but the class deserves the gate -- PORTABILITY.md already bans `| grep -q` under pipefail for the SIGPIPE variant, and this is its sibling: a grep whose empty result is the ordinary case, inside an assignment, under set -e. A first fix-probe here added || true and appeared not to help, which was a botched edit rather than evidence; the bash -x trace is what settled the mechanism.",
      "created_at": "2026-08-24T13:33:28Z",
      "updated_at": "2026-08-24T14:54:23Z"
    },
    {
      "id": "B3",
      "title": "BSD awk rejects a newline inside a string, so a table cell breaks plan-content on macOS",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "CI run 32394476430, job \"Test suite passes on macos-latest\": planning/tests/test-plan-commands.sh",
      "observed": "awk: newline in string | A | B |  — then the test failed (exit 64).",
      "expected": "The row is written as it is under GNU awk.",
      "mechanism": "plan_replace_paragraph passed the replacement text to awk with -v. A newline inside a -v assignment is what one-true-awk refuses outright, and GNU awk accepts, so a multi-paragraph or multi-line cell broke the write on macOS only.",
      "surfaces": [
        "planning/scripts/plan-content.sh",
        "planning/scripts/update-plan-content.sh"
      ],
      "fix": "9f41091 (codex): plan_replace_paragraph writes the replacement to a temp file and reads it in awk BEGIN instead of passing it with -v. Undocumented at the time -- attributed here by matching the change against the failure.",
      "verification": "CI run 32474258670 at a8d1285: both macOS legs pass, including test-plan-commands, which is the test that failed with \"awk: newline in string\" at run 32394476430. That is the first green BSD userland this branch has had. Confirmed again by run 32476867183, so the first green was not a one-off.",
      "found_by": "the macOS leg of CI, the first execution of this code on BSD userland",
      "notes": "Cannot be reproduced on this machine: it needs BSD awk. Diagnose from the CI log or a macOS runner, not by guessing here.",
      "created_at": "2026-08-20T17:17:13Z",
      "updated_at": "2026-08-21T11:28:09Z"
    },
    {
      "id": "B4",
      "title": "A plans-root path comparison fails on macOS, where TMPDIR carries a trailing slash and /var is a symlink",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "CI run 32394476430, macos-latest: planning/tests/test-plan-env.sh line 50",
      "observed": "assertion failed: grep -Fqx \"demo-plan|$plan_root/plan-description.md|$plan_root/steps\". The log shows paths like /var/folders/.../T//ai-skills-tests.XmQi3B — note the double slash.",
      "expected": "The helper output holds the plan paths exactly as the test builds them.",
      "mechanism": "The test compared a path it had built literally against one the code had resolved. On macOS /var is a symlink to /private/var, so pwd -P returns the resolved form and an -Fx comparison cannot match.",
      "surfaces": [
        "planning/tests/test-plan-env.sh",
        "planning/scripts/plan-env.sh"
      ],
      "fix": "9f41091 (codex): test-plan-env.sh canonicalises its temp root with tmp=\"$(cd \"$tmp\" && pwd -P)\" before building the expected strings, so both sides are resolved.",
      "verification": "CI run 32474258670 at a8d1285: test-plan-env passes on both macOS legs, having failed at line 50 in run 32394476430. Confirmed again by run 32476867183.",
      "found_by": "the macOS leg of CI",
      "notes": "The trailing-slash TMPDIR was the wrong half of my guess: the double slash in the log was cosmetic, and the symlink was the whole cause. Recorded because a plausible mechanism stated alongside the real one is what sends the next reader down the wrong path.",
      "created_at": "2026-08-20T17:17:13Z",
      "updated_at": "2026-08-21T11:28:09Z"
    },
    {
      "id": "B5",
      "title": "BSD grep rejects an empty subexpression, so the progress fallback pattern never matches on macOS",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "CI run 32394476430, macos-latest: planning/tests/test-progress-derivation.sh",
      "observed": "grep: empty (sub)expression, then FAIL: rebuild-plan-progress falls back to goal name for a bare goal.",
      "expected": "The row | 02-goal | 02-goal | is found.",
      "mechanism": "A pattern contains an empty alternation branch, which GNU grep tolerates and BSD grep refuses outright. Same family as the bre-alternation rule added today, and equally silent: the pattern matches nothing rather than erroring where it is built.",
      "surfaces": [
        "planning/tests/test-progress-derivation.sh",
        "planning/scripts/rebuild-plan-progress.sh"
      ],
      "fix": "The pattern is passed to check(), which greps with -E, so its pipes are escaped: a leading bare | is an empty alternation branch. One offender in the repo; the other helpers that take a pipe-leading pattern grep with a BRE, where | is literal, and are unaffected.",
      "verification": "test-progress-derivation passes, and the escaped pattern still matches the row. The unescaped form is rejected here too by ugrep (error at position 4), so the fix is strictly more portable, not merely macOS-specific.",
      "found_by": "the macOS leg of CI",
      "notes": "Candidate for a gated portability rule: an empty branch is textually detectable, unlike the general BRE-alternation case.",
      "created_at": "2026-08-20T17:17:13Z",
      "updated_at": "2026-08-20T17:27:05Z"
    },
    {
      "id": "B7",
      "title": "No maintainer document mentions the registers, so a change that resolves or finds a bug leaves BUGS.json and TODO.json untouched",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "grep -c \"BUGS.json\\|TODO.json\\|bug-report\" planning/MAINTAINER.md CONTRIBUTING.md AGENTS.md CODE-CONTRACTS.md DEVELOPMENT.md",
      "observed": "Zero in all five. MAINTAINER.md section 4, the per-change checklist, has eight steps covering consumers, fixtures, diagrams, docs, manifest/map/skill_files registration, which tests to run, and the commit shape — and no step touching either register.",
      "expected": "The checklist names the registers: an entry closed when its defect is fixed, an entry added when one is found and not fixed in the same breath, and the version stamp refreshed.",
      "mechanism": "The registers arrived late and were documented only in their own skills. Every other obligation in the checklist has a gate that fails without it, so the checklist is a convenience for those; for the registers it is the only instruction there is, and it is silent. Nothing fails when BUGS.json is stale.",
      "surfaces": [
        "planning/MAINTAINER.md",
        "CONTRIBUTING.md",
        "CODE-CONTRACTS.md",
        "AGENTS.md"
      ],
      "fix": "MAINTAINER.md section 4 gains step 8 (update the registers) and renumbers the commit step to 9, which now also says the message names the entries it closes. The artifact map gains a row for both registers. CONTRIBUTING.md gains a bullet in the pre-PR list, and AGENTS.md says to read the relevant register before starting and update it when finished.",
      "verification": "grep for BUGS.json across the five maintainer-facing docs went from 0 hits to 4. No gate is possible -- no test can tell that a commit resolved a defect -- and each of the four places says so, so the absence of enforcement is documented rather than implied. The doc gates (duplication ratchet, mermaid accuracy, comment format, persona drift) and blast-radius still pass.",
      "found_by": "reviewing commit 9f41091 (codex, \"planning: resolve bug report items\"): 40 files and 789 insertions, zero changes to BUGS.json or TODO.json",
      "notes": "Written immediately after the review that found it, and the same review is the evidence: commit 9f41091 met every gated obligation and missed this one. The temptation while fixing it was to defer closing this very entry because a subagent holds BUGS.json in another worktree -- which is the same excuse the step exists to remove, so it was closed here and the conflict left to be resolved.",
      "created_at": "2026-08-21T09:19:59Z",
      "updated_at": "2026-08-21T09:56:31Z"
    },
    {
      "id": "B8",
      "title": "add-ui-story-links.sh could not reject an unknown work unit on bash 3.2, because its guard ran in a pipeline subshell",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "nix develop --command bash32-run planning/tests/test-plan-commands.sh  # from commit 9f41091",
      "observed": "add-ui-story-links accepted an unknown related work unit. Passed under bash 5.3, failed on 3.2 only.",
      "expected": "W99 is not in the inventory, so the call refuses with exit 66.",
      "mechanism": "The validating loop was the last stage of a pipeline, so it ran in a subshell and plan_die exited that subshell rather than the script. On bash 5 set -e then aborts on the pipeline status; on bash 3.2 it does not. Isolated: `set -euo pipefail; printf a | while read x; do exit 66; done; echo CONTINUED` prints CONTINUED on 3.2 and aborts on 5.3.",
      "surfaces": [
        "planning/scripts/add-ui-story-links.sh"
      ],
      "fix": "Split on commas with IFS and a for loop, so the guard runs in the current shell. No other guard in the repo has this shape — checked.",
      "verification": "test-plan-commands passes on both shells; before the fix it passed on 5.3 and failed on 3.2. Catalogued as the pipeline-subshell-guard rule, documentation-only because the shape spans several lines and a pipeline-while is fine when it is not guarding.",
      "found_by": "reviewing commit 9f41091 (codex) — the bash 3.2 leg of ./verify-both-shells.sh",
      "notes": "The floor earning its keep: a guard that reads correctly and does nothing is worse than no guard, and only 3.2 showed it. Codex met every gated obligation in that commit and this one had no gate.",
      "created_at": "2026-08-21T09:27:06Z",
      "updated_at": "2026-08-21T09:27:06Z"
    },
    {
      "id": "B9",
      "title": "Every test install wrote its throwaway --target into the real XDG config of the user, 1894 lines of it",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "wc -l \"${XDG_CONFIG_HOME:-$HOME/.config}/tsch-ai-skills/custom-locations\"",
      "observed": "1895 lines, 120K: 1894 of them /tmp paths from test runs, one real entry.",
      "expected": "A one-off --target is not a location the user chose to remember.",
      "mechanism": "select_targets called save_custom_location on the non-interactive --target path, so every test that installed into a temp directory appended to a persistent user config file.",
      "surfaces": [
        "installer/src/40-selection.sh"
      ],
      "fix": "9f41091 (codex) removed the call. The interactive path still saves, which is where a user does choose a location.",
      "verification": "No new entries after a --target install; the interactive caller at 40-selection.sh:129 is untouched, so the feature survives.",
      "found_by": "reviewing 9f41091 - the diff removed a call, and the file it wrote to turned out to hold 120K of test debris",
      "notes": "The stale entries are gone: pruned 2026-08-21 from 1895 lines to the single live one, with a backup at custom-locations.bak. Measured before pruning: only 1 of the 1895 directories still existed, so load_custom_locations already filtered the rest with [ -d ] and the menu was never visibly wrong. The objection was the write itself, plus the fact that a leaked test root that did still exist would have been offered as an install target.",
      "created_at": "2026-08-21T09:34:37Z",
      "updated_at": "2026-08-21T09:45:02Z"
    },
    {
      "id": "B10",
      "title": "The per-test TMPDIR leaked for 67 of the tests, because a test setting its own EXIT trap replaces the one lib-test.sh installs",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "source planning/tests/lib-test.sh; trap 'rm -rf x' EXIT; exit 0   # then look for /tmp/t.?????",
      "observed": "The root survives. 67 test files set their own EXIT trap, and six stale roots were sitting in /tmp.",
      "expected": "Each test root is removed when the test ends.",
      "mechanism": "lib-test.sh installs an EXIT trap at source time; bash keeps one EXIT trap per shell, so a test that traps EXIT afterwards silently discards it. Commit 1044857 introduced this, and the assertion written with it only covered a shell that sets no trap of its own.",
      "surfaces": [
        "planning/tests/lib-test.sh",
        "run-tests.sh"
      ],
      "fix": "9f41091 (codex): run-tests.sh exports AI_SKILLS_TEST_RUN_ID, lib-test.sh writes it into each root as .ai-skills-test-run-id, and the runner sweeps only roots carrying this run id.",
      "verification": "A run leaves no /tmp/t.????? behind. The marker keeps the sweep safe under concurrency, which was the objection behind refusing to sweep at all.",
      "found_by": "reviewing 9f41091 - its comment named the trap hazard, and the reproduction confirmed it",
      "notes": "Better than the decision recorded in T10: a run was said to be unable to distinguish its own leftovers from a concurrent run live directories. A run id distinguishes them.",
      "created_at": "2026-08-21T09:34:37Z",
      "updated_at": "2026-08-21T09:34:37Z"
    },
    {
      "id": "B13",
      "title": "On stock macOS the registry fallback runs a real benchmark agent with no time bound at all",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "On a host with neither setsid nor timeout/gtimeout: start-worker.sh calls launch_agent isolated \"${WORKER_TIMEOUT:-45m}\" - mode becomes registry, no timeout binary is found, and lib-agent.sh warns and launches anyway.",
      "observed": "lib-agent.sh:140-148 prints \"registry fallback cannot bound the agent to 45m\" and then runs the agent unbounded, because cmd gets no timeout prefix.",
      "expected": "A worker that cannot be bounded is refused, as it was before: the setsid path still returns 69 in the same situation.",
      "mechanism": "9f41091 added an isolated mode that degrades setsid to a registry fallback when setsid is missing, which is stock macOS. The same change turned the missing-timeout refusal into a warning for that fallback only. The two degradations compose: no process group and no clock.",
      "surfaces": [
        "benchmark/planning/runtime/lib-agent.sh",
        "benchmark/planning/case/start-worker.sh"
      ],
      "fix": "lib-agent.sh grew a third rung below timeout/gtimeout: in registry mode only, launch_agent parses the spec (<n>s/<n>m/<n>h, 64 on anything else) and starts _agent_watchdog as a background sibling of the agent. The watchdog polls kill -0 in one-second steps, then TERMs the ps-walked tree and escalates to KILL after 5s; wait_agent calls _agent_watchdog_stop, which kills the watchdog and its in-flight sleep. The setsid path still returns 69, and a present timeout/gtimeout still wins, so the watchdog is never used when a real bound exists.",
      "verification": "test-safeguards.sh launches fake agents with a PATH holding neither timeout nor gtimeout: a 2s-bounded sleeper and its descendant are both killed, a TERM-ignoring agent is KILLed, an agent that exits on its own is not killed and leaves no watchdog or sleep, 45x is refused with 64, a stubbed timeout binary is used with no watchdog started, and the setsid path still returns 69. Mutations that each fail exactly one of those: never starting the watchdog (the B13 regression), TERMing the pid instead of the tree, dropping _agent_watchdog_stop, TERMing the watchdog without its sleep child, one long sleep instead of the poll, accepting any spec, skipping the timeout binary, starting the watchdog even when timeout is present, and turning the setsid 69 back into a warning.",
      "found_by": "reviewing 9f41091 after being asked about the setsid fix specifically",
      "notes": "These callers launch real worker and reviewer agents that spend real tokens, so an unbounded run is the expensive failure, not an inconvenient one. The new tests in test-safeguards.sh cover the registry wiring and prove the nohup/setsid/open shims refuse, but assert nothing about the timeout path. Options: keep the refusal in registry mode too; or implement a shell watchdog (background sleep plus kill of the registered pid and its ps-walked descendants) so the bound survives without GNU coreutils. The second is more work and is the only one that makes a macOS benchmark run at all.",
      "created_at": "2026-08-21T09:46:39Z",
      "updated_at": "2026-08-21T10:17:50Z"
    },
    {
      "id": "B15",
      "title": "--yes did not answer the planning permission prompts, so a headless install died on a question",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "HOME=/tmp/fresh bash install.sh --skill planning --target /tmp/t --yes </dev/null",
      "observed": "Create /tmp/fresh/.plans as the global plans directory? [y/N/a] install.sh: line 296: read: 3: invalid file descriptor: Bad file descriptor / Error: Interactive input is required, exit 1.",
      "expected": "--yes answers the prompt and the install completes.",
      "mechanism": "confirm() tested YES_ALL, which only the interactive \"a\" answer sets, and never YES, which is what --yes sets. Its help text scoped --yes to replacements, but README sells it for headless and CI use, and a flag for headless runs that stops on a question defeats itself. Separately, fd 3 is deliberately closed when there is no tty, so bash printed its own invalid-file-descriptor complaint before ask() could die with the actionable message.",
      "surfaces": [
        "installer/src/15-input-channel.sh",
        "installer/src/05-config.sh"
      ],
      "fix": "confirm() honours YES as well as YES_ALL, and the help line says --yes answers every prompt including the planning permission grants -- the widening is documented rather than silent, since those prompts grant agents read/write and script execution. ask() suppresses bash own read error so only the die message is shown.",
      "verification": "test-installer-noninteractive.sh: a planning install under --yes exits 0, asks for nothing, creates the plans directory and installs the skill; without --yes it exits 1 with the actionable message and no raw shell error; and a second --yes run still writes the scripts, so yes-to-everything cannot be implemented as do-nothing. Mutations: restoring the YES_ALL-only test fails the first two assertions, and removing the 2>/dev/null fails the no-shell-noise assertion. CI run 32476867183 exercises it on ubuntu and both macOS legs.",
      "found_by": "reported by Michael. I had hit the same failure earlier in the session while installing planning to a temp target and worked around it with a pipe instead of investigating.",
      "notes": "Two things I first called defects here were not. The exit code was correct all along -- my measurement read the status of a pipe. And the summary printed after the failure is deliberate: cleanup() prints it so a run that dies part-way still reports what it wrote, and says so in a comment. HOME has to be redirected to see any of this, because the prompt only appears when the plans directory does not exist, which on a developer machine it usually does.",
      "created_at": "2026-08-21T11:05:01Z",
      "updated_at": "2026-08-21T11:28:09Z"
    },
    {
      "id": "B16",
      "title": "verify-both-shells.sh deletes a concurrent run's live worktree, failing that run wholesale",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "Start ./verify-both-shells.sh, and while it runs start a second one. The second sweeps the first worktree away.",
      "observed": "The first run reported 87 tests, 31 passed, 56 failed on its bash 3.2 leg, with errors like \"cd: /tmp/verify-wt.PLtaGy/tree/planning/scripts: No such file or directory\" and \"/tmp/verify-wt.PLtaGy/tree/install.sh: No such file or directory\". Its bash 5.3 leg, which had finished before the second run started, passed all 87.",
      "expected": "A second run leaves a live worktree alone and sweeps only leftovers.",
      "mechanism": "The startup sweep removes every registered worktree matching verify-wt.*, on the assumption that any it finds is a leftover from a killed run. It cannot tell a leftover from a live one, so a concurrent run deletes the tree the first is executing in, file by file, underneath it.",
      "surfaces": [
        "verify-both-shells.sh"
      ],
      "fix": "already in tree (pid beside the worktree, sweep skips a live owner); verified this session, see verification",
      "verification": "Planted three registered verify-wt.* fixtures -- one whose harness.pid is a live sleep, one with pid 999999, one with no pid file -- then ran ./verify-both-shells.sh once. Output: leaving live worktree ...livefix (harness pid N), sweeping leftover ...stalefix and ...stalefix2; after the run only the live fixture remained registered. The registration-before-pid-write race cannot happen: a run registers its worktree only after its pid file exists.",
      "found_by": "the verification of c565658, which I started and then started a second one for the next commit while it was still running",
      "notes": "Same class as the test-root sweep in run-tests.sh, which codex fixed with a run-id marker after I had concluded a run could not distinguish its own leftovers from a concurrent run live directories. I wrote that reasoning down for run-tests.sh and did not apply it to my own harness. A fix is in the working tree, unverified: each run writes its pid beside its worktree and the sweep skips any whose owner is still running. Proving it means planting a worktree owned by a live process and running the sweep, which is invasive enough to want its own moment rather than being folded into another commit. Until then the rule is one verification at a time, and a wholesale failure with missing-file errors is this, not a regression in the code under test. CI cannot close this one: it runs a single suite per job, so nothing there exercises two concurrent verifications. The proof has to be local and deliberate.",
      "created_at": "2026-08-21T11:21:03Z",
      "updated_at": "2026-08-24T08:47:14Z"
    },
    {
      "id": "B18",
      "title": "dep_reaches memoizes every visited pair before exploring and reads the memo back as failure, so an earlier probe of a pair turns a later valid check into a false FAIL",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "In one goal give two verification steps prose naming each other's unit id (W17 with W15 in its Depends-on cell, W15's step mentioning W17), then run validate-plan.sh <plan>. Isolated: source the propagation lib, populate the unit_depends/unit_type/unit_goal maps for both edges, set dep_seen=() once, and call plan_validate_propagation_reach.",
      "observed": "\"W17 is a verification unit that grades W15 but has no dependency path to it\" although work-unit-inventory.md lists W15 in W17's Depends-on cell. A standalone probe of dep_reaches on the same pair returned success.",
      "expected": "A pair with a satisfiable edge in either direction counts as ordered.",
      "mechanism": "dep_reaches does plan_map_has dep_seen \"$key\" && return 1 before exploring and then plan_map_set dep_seen \"$key\" 1 immediately, so the map records visited-but-not-outcome while every hit is read as known-failure. Within one pair check the elif reverse probe (dep_reaches named id) runs against the memo the failed forward attempt left dirty — dep_seen=() is reset only before the forward attempt — and a key first written during an earlier successful exploration answers a later direct check with return 1.",
      "surfaces": [
        "planning/scripts/validate-plan-propagation-lib.sh"
      ],
      "fix": "8383072 — dep_reaches keeps two maps: dep_seen marks pairs whose walk is in progress and cuts cycles, dep_failed caches only walks that completed without reaching the target; the caller clears both with plan_map_clear before each query instead of assigning an unrelated variable named dep_seen (the maps live in plan_map__dep_seen__<key> variables).",
      "verification": "planning/tests/test-verifier-reach-memo.sh: two same-goal verifiers whose steps name each other, joined by a direct reverse dependency edge, produced \"W04 ... no dependency path\" against HEAD and produce none now; its control asserts a real grader gap is still reported. Mutation: reverting validate-plan-propagation-lib.sh to the pre-fix version reproduces the false finding.",
      "found_by": "ox-alpha exercising the planning skill on the health-app VPS (bugs-found-by-ox-alpha.md #1)",
      "notes": "Worked around on the VPS by authoring around the validator: never mention another unit's WNN inside verification-step narrative so no cross-pair probes fire. The suggested fix from the report: treat the memo as a failure cache only — set keys on the failure paths, or clear the key before returning success.",
      "created_at": "2026-08-22T16:21:07Z",
      "updated_at": "2026-08-22T17:08:20Z"
    },
    {
      "id": "B19",
      "title": "update-adversarial-review.sh parses every line of adversarial-review-incoming.md as a CSV row, so a reviewer title/header preamble gets the findings rejected",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "Write adversarial-review-incoming.md as: a '# Adversarial review incoming findings - <persona>' title line, the header row ID,Missing or over-broad item,Required plan change,Status,Work unit, then finding rows; run update-adversarial-review.sh <plan> (the incoming file is consumed automatically when present).",
      "observed": "\"CSV row 2 has 1 columns, expected 5\" — the named row points into the data, while the offending line is the preamble above it. The exact same rows parse cleanly once everything above the ID,... line is stripped and the file is passed via --file.",
      "expected": "Title/comment lines, blank lines, and the reviewer's own copy of the canonical header row are skipped, and the findings render.",
      "mechanism": "The whole incoming file is copied to csv_file and handed to plan_render_csv_table, which demands every non-empty line be a full 5-column row. That contract fits the stdin/--file sources (bare rows) but not the documented reviewer write format this consumer exists to read.",
      "surfaces": [
        "planning/scripts/update-adversarial-review.sh"
      ],
      "fix": "f2ee108 — update-adversarial-review.sh filters its CSV source before rendering: title/comment lines starting with #, blank lines, and one repeated copy of the canonical header row are skipped whatever the source; a preamble-only document is refused with 65 naming the missing rows.",
      "verification": "planning/tests/test-adversarial-review-preamble.sh: an incoming file holding title + header + three rows (one after a blank line) renders all three rows, exactly one header, and no title text against adversarial-review.md — against HEAD it exited 65 with \"row 2 has 1 columns\". Controls: bare rows via stdin still render, and a preamble-only document keeps a nonzero exit now naming the real fault. Mutation: reverting the script to HEAD fails the first assertion.",
      "found_by": "ox-alpha exercising the planning skill on the health-app VPS (bugs-found-by-ox-alpha.md #2)",
      "notes": "Reviewers following the documented write format get their findings rejected with an error that mislocates the fault. Workaround on the VPS: strip everything above the header line into a temp CSV and use --file.",
      "created_at": "2026-08-22T16:21:07Z",
      "updated_at": "2026-08-22T17:18:11Z"
    },
    {
      "id": "B20",
      "title": "add-work-unit.sh moves the inventory row and step file into place before editing the goal roster, so a roster failure strands a half-created unit whose retry is refused",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "Take a goal whose Owned work units section holds no numbered paragraphs (e.g. after B21), then run add-work-unit.sh <plan> --id W05 --type docs --file N/A --scope x --subscope N/A --change y --depends-on - --goal <goal> --step <step>; repeat the identical call.",
      "observed": "First call writes the inventory row and the step file, then dies with \"Goal has no numbered Owned work units section\". The retry dies earlier with \"Work-unit ID already exists\": row and step exist, the roster entry does not.",
      "expected": "All three writes land together or none does, which is what this script's docblock promises.",
      "mechanism": "The inventory and step temp files are mv'd into place before the goal-roster edit runs; that edit can still plan_die (no numbered §9.x section, no paragraph to append after). Only the goal temp is discarded by the trap — the two completed renames are never undone.",
      "surfaces": [
        "planning/scripts/add-work-unit.sh"
      ],
      "fix": "The goal-roster edit is staged into its temp before the inventory and step renames: the placeholder branch copies the goal and runs plan_replace_paragraph on the copy; the scan branch writes the insert awk output to the temp. All three mv statements now sit together after every fallible step, so a roster failure leaves the plan byte-identical.",
      "verification": "planning/tests/test-add-work-unit-staging.sh: against HEAD a refused call left both the W02 inventory row and 02-step-b.md behind; with the fix the refusal message is unchanged and the plan holds no trace of the call. Controls prove the healthy-goal path still lands all three writes. Mutation: whole-revert to HEAD reproduces both stranded artifacts.",
      "found_by": "ox-alpha exercising the planning skill on the health-app VPS (bugs-found-by-ox-alpha.md #3)",
      "notes": "Recovery today is undocumented and manual: recreate the roster paragraph via update-plan-content.sh owned-work-units, then use update-work-unit.sh for the stranded id. Contract 2 (irreversible last) is the rule this violates.",
      "created_at": "2026-08-22T16:21:07Z",
      "updated_at": "2026-08-22T19:50:57Z"
    },
    {
      "id": "B22",
      "title": "update-adversarial-review.sh rewrites the Findings table and archives history before mint-fix-keys.sh runs, so a gated-row failure leaves the table replaced without keys",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "Feed findings whose Work unit cell is 'W01-W18' through adversarial-review-incoming.md (or stdin); run update-adversarial-review.sh <plan>; then compare fix-keys.json, adversarial-review.md and adversarial-review-history.md against their pre-call state.",
      "observed": "\"WARN skipping gated row ... could not be minted\" and a nonzero exit; fix-keys.json was not written — but the Findings table rewrite and the history archive had already been applied. The approval gate later demands claims for keys that were never minted.",
      "expected": "No user-visible state changes unless the keys were minted: the irreversible mutation comes last (CODE-CONTRACTS.md contract 2).",
      "mechanism": "mint-fix-keys.sh is invoked as the script's final statement, after the history append, the review-file rename, and the incoming-file removal. Any of its refusals — a gated row with non-conforming ids, openssl unavailable — therefore lands on top of completed mutations.",
      "surfaces": [
        "planning/scripts/update-adversarial-review.sh",
        "planning/scripts/mint-fix-keys.sh"
      ],
      "fix": "5c047d9 — update-adversarial-review.sh stages the rewrite into its temp file, mints that staged copy against a throwaway plan directory, and only then archives history, renames the review file, removes the incoming file, and runs the real mint; a refusal forwards mint exit code and adds the remedy.",
      "verification": "planning/tests/test-adversarial-review-mint-order.sh: against HEAD the unmintable row rewrote adversarial-review.md, archived a history cycle and consumed the incoming file; with the fix all three files stay byte-identical, no fix-keys.json appears, the repaired retry completes end to end, and direct mint invocations still refuse. Mutation: whole-revert to HEAD reproduces the three symptoms.",
      "found_by": "ox-alpha exercising the planning skill on the health-app VPS (bugs-found-by-ox-alpha.md #5)",
      "notes": "Same class as the contract 2 incident where --review-status approved destroyed the session secret before its own guard sites. Workaround on the VPS (normalize Work unit cells before running) is no longer needed for safety, though clean cells still avoid refusals. Sequenced first at the health-app worker request; they had hit this twice on 2026-08-22.",
      "created_at": "2026-08-22T16:21:07Z",
      "updated_at": "2026-08-22T19:42:10Z"
    },
    {
      "id": "B23",
      "title": "verify-fix-keys.sh self-certification gate cannot pass when reviewers share the coordinator's session id, so a legitimate claim reads as self-certified",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "In an opencode harness: coordinator mints (or a subagent reviewer re-mints) with the shared derived session id, then the fixer claims via add-fix-claim.sh and verify-fix-keys.sh --claimed-by <session> runs — minted_by equals the claimed session for every pair, which the approval gate treats as self-certification.",
      "observed": "minted_by == claimed session no matter who ran mint, because subagent reviewers share the coordinator's derived session id; the gate cannot distinguish roles. Workaround used on the VPS: the documented MINTED_BY override, attributing the mint to the reviewer role.",
      "expected": "Either an explicit MINTED_BY is accepted as legitimate attribution, or the harness limitation is documented next to the gate so operators know the check is advisory there.",
      "mechanism": "The session id names the secret dir and is derived per plan, not per agent; every role in one harness session derives the same id, so the minted_by/claimed-by equality the gate checks holds identically for honest and self-certifying flows.",
      "surfaces": [
        "planning/scripts/verify-fix-keys.sh",
        "planning/scripts/mint-fix-keys.sh"
      ],
      "fix": "Option (a) of the worker write-up: the explicit MINTED_BY override is the legitimate attribution path and is now documented at the gate itself — the self-certification refusal names re-minting with MINTED_BY=<reviewer identity> as the shared-session-id remedy, and verify-fix-keys docblock states it. No behaviour change: MINTED_BY attribution and its cross-role pass were already implemented and mutation-tested in test-fix-keys.sh.",
      "verification": "test-fix-keys.sh already pins both sides: a claim whose --claimed-by equals an explicitly overridden minted_by fails as self-certified, and a claim from a different session passes. test-fix-keys passes after the wording change; the remedy in the message is the flow the health-app worker used successfully across cycles 1-4.",
      "found_by": "ox-alpha (<HEALTH> worker) reply in ../health-app/bug-fixed/fixed-by-ai-skills.md, 2026-08-22; details pending append to bugs-found-by-ox-alpha.md (#6)",
      "notes": "Decision needed before fixing: accept explicit MINTED_BY as legitimate when set (weaker gate), or document that the self-certification check is only meaningful across distinct session secrets. Recorded from the worker's report; reproduction not yet run locally.",
      "created_at": "2026-08-22T19:42:10Z",
      "updated_at": "2026-08-22T20:49:08Z"
    },
    {
      "id": "B24",
      "title": "add-planning-bug's damaged-register guard relies on jq 1.7 empty-input behaviour, so under jq 1.6 a zero-byte register reports success",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "Stub a jq on PATH that exits 0 silently when any argument equals the --arg name 'now' (planning/tests/test-add-planning-bug.sh does exactly this), then run add-planning-bug.sh on a plan with no planning-bugs.json under jq-1.6.",
      "observed": "The stubbed append writes a zero-byte register; the read-back guard jq -e on an empty file exits 0 under jq-1.6 (no input means no output, so -e never gets a false/null last output), the writer prints Recorded, and test-add-planning-bug fails its three refusal assertions. The same test passed where CI runs jq >= 1.7, whose -e exits 4 on no output.",
      "expected": "A write that produced nothing is refused with 70 naming the register and id, whatever jq version reads it back.",
      "mechanism": "The guard delegated the emptiness check to jq -e's no-output exit status, which changed between jq 1.6 and 1.7 instead of being decided by this script.",
      "surfaces": [
        "planning/scripts/add-planning-bug.sh"
      ],
      "fix": "add-planning-bug.sh checks the register itself before the read-back: a zero-byte result is refused with the same 70 message naming register and id. The guard no longer depends on jq -e empty-input behaviour, which differs between jq 1.6 (exit 0) and 1.7 (exit 4).",
      "verification": "test-add-planning-bug.sh fails at 885cba2 and on this branch pre-fix under jq-1.6 with three refusal assertions; it passes with the guard. Mutation: removing only the [ -s ] line reproduces all three failures under jq-1.6. The existing assertions were already mutation-hardened in B12 work; no new test needed — the seam test was right, the guard was version-dependent.",
      "found_by": "the full-suite run on this machine after the B18-B22 fixes: test-add-planning-bug failed identically at 885cba2, before this branch touched anything",
      "notes": "Second site of the same class, not fixed here for scope: validate-plan-commands-lib.sh line ~29 answers extension registration with jq -e index() against the registry file — an empty never-executable-extensions.json would read as every extension registered under jq-1.6. That registry ships non-empty and is gate-checked elsewhere, so the exposure differs from a caller-supplied write; worth hardening if it is ever regenerated empty.",
      "created_at": "2026-08-22T20:20:09Z",
      "updated_at": "2026-08-22T20:21:22Z"
    },
    {
      "id": "B30",
      "title": "chat promises exchange across machines, but every server tier binds 127.0.0.1 with no way to change it",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "grep -n '127\\.0\\.0\\.1' chat/runtime/server.py chat/runtime/server.pl chat/runtime/server.js chat/scripts/chat-server.sh; then grep -n 'bind\\|--host' chat/scripts/chat-server.sh",
      "observed": "server.py binds (\"127.0.0.1\", port), server.pl sets LocalAddr => '127.0.0.1', server.js listens on \"127.0.0.1\", and the socat tier uses TCP-LISTEN:...,bind=127.0.0.1. chat-server.sh has no bind or listen-address option: its only network flag is --port. Meanwhile chat/SKILL.md's description says the skill is for when agents need to exchange messages \"across sessions or machines\", and every client helper takes --host.",
      "expected": "Either a machine other than the server's own can reach it, or the documentation does not offer that.",
      "mechanism": "The loopback bind is hard-coded in all four tiers and no flag reaches it. --host on the client side therefore only ever resolves to a server on the same machine, or to one reachable through a tunnel that nothing in the skill sets up or mentions. The promise and the socket disagree, and the socket is right.",
      "surfaces": [
        "chat/SKILL.md",
        "chat/scripts/chat-server.sh",
        "chat/runtime/server.py",
        "chat/runtime/server.pl",
        "chat/runtime/server.js"
      ],
      "fix": "c36bab8 — --bind ADDR sets AI_CHAT_BIND; socat option and the python3/node/perl listeners read it, status reports server.bind, and SKILL.md documents the unauthenticated-protocol caveat for non-loopback binds",
      "verification": "Contract pins cover all four runtime sites; an explicit-bind roundtrip runs on the python3 tier in test-chat.sh. A real cross-machine exchange needs two hosts and stays a documented manual step.",
      "found_by": "reading the diff of the -ox branch: the description was checked against the sockets rather than taken at its word",
      "notes": "Two ways out, and they are not equivalent. Correcting the description is the cheap one and probably the right one: the skill states it has no auth, so a loopback-only bus is a sane default and offering a listener on a routable address without authentication would be worse than the missing feature. Making the bind configurable is the other, and it should not be done without deciding what protects it -- at minimum a documented warning, and the four tiers each need the change since the address is hard-coded in each. Whichever is chosen, the cross-machine claim must not survive unqualified in the description, because an agent selecting this skill reads that line to decide whether it fits.",
      "created_at": "2026-08-24T12:23:50Z",
      "updated_at": "2026-08-24T18:58:14Z"
    },
    {
      "id": "B34",
      "title": "The overview counts only archived review rounds, so a plan whose review happened reports zero",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "bash planning/scripts/render-plan-overview.sh .plans/reviewer-oracle-evidence-hardening --out /tmp/o.html   # then read the header and the CYCLES panel",
      "observed": "A delivered plan -- review approved, all 16 steps complete, findings 1 (0 open), 1/1 resolved -- reports review rounds 0, review depth 0/3, and a CYCLES panel reading \"no review cycles recorded yet - the chart lights up after cycle 1\". The same plan's adversarial-review.md carries an approved verdict and one finding row.",
      "expected": "Two rounds are always present -- the pre-implementation review and the final review -- so a plan that has been reviewed reports 2, and 3 only when a third round was actually needed. The depth ring counts against that: 2 of 2 is a complete review, not a failure against a target of 3.",
      "mechanism": "render-plan-overview.sh:153-154 sets cycles=0 and then counts '^## Cycle [0-9]' headings in adversarial-review-history.md. That file only exists once a round has been archived, which update-adversarial-review.sh does when a *new* round starts. A plan with exactly one round therefore has no history file and counts zero, and every figure derived from cycles -- the rounds count, the review-depth ring against its target of 3, and the chart -- reports as though no review occurred. The undercount is by one everywhere, and it is worst on a finished plan, where the reader concludes the work was never reviewed.",
      "surfaces": [
        "planning/scripts/render-plan-overview.sh",
        "planning/scripts/update-adversarial-review.sh"
      ],
      "fix": "this session — render-plan-overview builds one normalised cycle stream from history plus (only when no history exists) the live table, filtering placeholder rows and dropping cycles they empty; count, ring, panel and chart all read that stream, so they cannot disagree",
      "verification": "test-plan-overview pins the live-only round: one round counted, chart bar drawn, placeholder absent. Mutations: forcing cycles=0 fails the narration assertion; counting placeholder rows fails the fresh-plan placeholder assertion.",
      "found_by": "Michael, reading the delivered render: review approved and all steps complete, yet feedback and cycles empty",
      "notes": "The live round is the missing term: rounds are archived history plus the current review when one exists. Both screenshots show it -- the planning plan reports 7 findings with 0 rounds while its phase line says round 1 is open, and the delivered plan reports a resolved finding with 0 rounds. Whatever counts a round must agree with the phase line, which already says \"round $((cycles + 1))\" and so has the correct number in hand. Worth deciding at the same time whether review depth against a target of 3 is meaningful for a plan that needed one round and got approval; a ring reading 0/3 on an approved plan reads as a failure rather than a completed review. Corrected by Michael: the model is not \"archived cycles plus the live one\" but a fixed pair -- pre review and final review -- extended when the work demands another. So the count does not come from counting history headings at all; the history file says how many rounds happened beyond the pair. That also fixes the depth ring, which currently reads 0/3 on an approved plan and implies a failure: it should read 2/2 for the normal case and 3/3 when a third was needed, never a target the plan was never meant to hit.",
      "created_at": "2026-08-24T13:03:57Z",
      "updated_at": "2026-08-24T15:45:14Z"
    },
    {
      "id": "B37",
      "title": "validate_requires_groups grew past the 40-line cap, so the ratchet gate is red on every leg",
      "severity": "major",
      "priority": "high",
      "status": "fixed",
      "found_by": "watch tick on CI run 32737475241 (head 7028a9b)",
      "created_at": "2026-08-24T14:27:49Z",
      "updated_at": "2026-08-24T14:44:04Z",
      "reproduce": "bash planning/tests/test-function-length-ratchet.sh",
      "expected": "exit 0: the over-cap count stays at or below CAP=67",
      "observed": "exit 1: \"function-length-ratchet: 68 function(s) exceed the 40-line cap (cap 67). New over-cap code must be split or the cap lowered deliberately.\" — installer/build.sh:99 validate_requires_groups() is listed at 45 lines. Fails on all three CI legs (ubuntu-latest, macos-latest, macOS bash 3.2).",
      "mechanism": "4befd4e (installer: the verification reds name themselves, and the gates accept the group tables) grew validate_requires_groups() from 24 to 45 lines. Confirmed: `git show 89ec358:installer/build.sh` measures the function at 24 lines, HEAD at 45. Crossing 40 adds a new over-cap site, taking the count 67 -> 68. test-function-length-ratchet pins CAP=67 and documents that the count may shrink but never grow, so the new site is a hard fail. The same commit fixed test-install-ui and test-portability-contract, which is why this is the only remaining new red.",
      "surfaces": [
        "installer/build.sh",
        "planning/tests/test-function-length-ratchet.sh"
      ],
      "notes": "The ratchet header says a genuine split lowers the cap in the same commit and the cap is never raised, so the fix is to split validate_requires_groups, not to bump CAP to 68.",
      "parent": null,
      "fix": "fa97d9f and follow-ups — validate_one_requires_manifest carries the per-skill checks (24 lines back under cap) and run_reviewer split into five helpers; cap follows the count down to 66",
      "verification": "test-function-length-ratchet passes at CAP=66 and its growth branch still fails on a planted 45-line function (mutation from the ratchet landing); validate_requires_groups measures under 40 at HEAD."
    },
    {
      "id": "B38",
      "title": "An executing agent can leave the plan describing a world that no longer exists, and nothing in verification notices",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "Execute any real work order under SKILL.md rules in a consumer repo: commit code for steps without calling update-step.sh in the same turn; implement an unplanned requirement and describe it in prose instead of add-work-unit.sh; deliver a step through a path outside the planned files. Then bash planning/scripts/validate-plan.sh <plan> — it passes or fails only on unrelated structural grounds, while progress.md still reads 0% and the inventory omits shipped units.",
      "observed": "Two health-app plans, cycle-15 era. order-split-api-attribute read 0% with all nine steps incomplete although the code was committed and REST-verified; EmployeeSplitSpecificationInterface and its model shipped with no unit; goal 05 of order-split-extended-rendering-v2 carried delivered work (order PDF, quote PDF) with no units at all. The executor listed every one of these as outstanding in its per-burst summaries, and the user caught the drift twice in one night.",
      "expected": "Completing work and recording it are one act. Verification must make plan-versus-reality drift visible when it is created — at the turn where code lands without its plan mutation — rather than depending on the operator reading both sides and catching it.",
      "mechanism": "The rules exist and are explicit: the progress-accuracy operating rule, §4.1.1 (dynamic scope additions are plan mutations), §2.5 (one step per unit). Nothing executes them. validate-plan checks structure before/during approval, never sync during execution; no gate connects target-repo changes to inventory, step or progress mutations; the executor summary ritual (reporting outstanding bookkeeping) made deferral feel like disclosure. Executor self-analysis names the habit: each burst optimised for visible code progress, treating the plan as a report written afterwards instead of live state mutated at the moment of discovery.",
      "surfaces": [
        "planning/SKILL.md",
        "planning/EXECUTION.md",
        "planning/scripts/validate-plan.sh",
        "planning/scripts/validate-plan-propagation-lib.sh"
      ],
      "fix": "this session — the freshness pass makes code-after-record drift visible at validation time (see T44); the summary-contract half lives in the execution rule that outstanding lists are mutations to perform, not disclosures",
      "verification": "test-plan-freshness: control silent, drift named with unit and dates, catch-up silent, outside-repo target skipped. Mutation inverted comparison fails the drift-named assertion.",
      "found_by": "user, health-app order-split execution sessions, August 2026 — drift caught manually twice after REST-verifying completed work",
      "notes": "Deliberate non-actions in the same session are honest decisions, not defects: W05 rewritten to what this repo controls (its trigger lives behind DownloadInvoice::execute() curling Profortool), reviews left pending to avoid self-approval. The defect is the silent drift class. Related trust surface: B34 — an overview that undercounts review rounds is the same failure family, records disagreeing with reality while looking authoritative.",
      "updated_at": "2026-08-24T18:08:32Z",
      "created_at": "2026-08-24T15:00:31Z"
    },
    {
      "id": "B39",
      "title": "install.sh with no --target exits 1 printing nothing at all, so a headless install fails silently",
      "severity": "major",
      "priority": "high",
      "status": "fixed",
      "found_by": "installing the new git-worktrees skill without --target",
      "created_at": "2026-08-24T16:30:16Z",
      "updated_at": "2026-08-24T18:58:14Z",
      "reproduce": "bash install.sh --skill git-worktrees --yes; echo \"exit $?\"   # exit 1, zero bytes\nbash install.sh --skill git-worktrees --target \"$HOME/.claude/skills\" --yes; echo \"exit $?\"   # exit 0, installs",
      "expected": "either the default target is used and the skill installs, or a diagnostic names what is missing; a non-zero exit with no message is never acceptable",
      "observed": "exit 1 with 0 bytes captured from 2>&1, both with stdout on a pipe and redirected to /dev/null. The same command with an explicit --target exits 0 and prints 400 bytes ending 'Installed: /home/mdibbets/.claude/skills/git-worktrees'.",
      "mechanism": "Not the target logic. install.sh:3552 calls show_splash only when SKILL_SELECTION or TARGET_SELECTION is empty, which is why passing --target hides the bug. show_splash (install.sh:1077) starts with a bare 'return' guarded by a tty test on fd 3; with no tty the test fails and the bare return propagates status 1. show_splash is the last command in that then-branch, so under set -euo pipefail (install.sh:5) the script aborts there, before select_targets runs, printing nothing. Isolated proof: a function whose body is the same tty test plus bare return exits 1 and never reaches the next statement, while the same function with 'return 0' reaches it.",
      "surfaces": [
        "install.sh",
        "installer/src (the show_splash source and its call site)",
        "tests/test-install-ui.sh"
      ],
      "notes": "Hits every headless install that omits --target: CI, the one-liner, an agent installing for a user. Same family as B15, the non-interactive path is not exercised by the suite. A regression test should assert that a --yes install with no --target either succeeds or prints a diagnostic, and should fail if a bare return is reintroduced in show_splash.",
      "parent": null,
      "fix": "this session — show_splash says return 0 on every early exit, and a --yes run with no interactive channel takes the target menu default (first listed root) with a printed note instead of dying in ask",
      "verification": "Sandboxed headless install without --target exits 0, prints the no-channel note and Installed: path. Regression pinned in test-installer-noninteractive.sh: exit must be 0, output non-empty."
    },
    {
      "id": "B40",
      "title": "add-work-unit silently destroys a goal progress record (statuses reset, plan row flips)",
      "status": "fixed",
      "severity": "major",
      "priority": "high",
      "parent": "B38",
      "reproduce": "Goal with 01-step-one completed and 02-step-two in progress: bash planning/scripts/add-work-unit.sh $P W03 test t/CTest.php 'CTest::testThree()' N/A 'test three' W02 01-alpha 03-step-three; then cat $P/01-alpha/progress.md — both rows read incomplete",
      "observed": "Both step statuses gone; on a fully-complete goal the plan-level row flipped completed to incomplete. No warning printed; git snapshot silently no-ops in a gitignored plans root so recovery is empty",
      "expected": "Adding a unit amends the tracker: statuses carry across by step name; at minimum print the same rebuilt-progress note remove-work-unit.sh prints",
      "mechanism": "add-work-unit.sh calls plan_rebuild_goal_progress which rm -f the progress file and recreates it via create-progress.sh before any status is read; plan_rebuild_plan_progress then recomputes the plan-level row from the wiped tracker",
      "surfaces": [
        "planning/scripts/plan-reconcile-lib.sh",
        "planning/scripts/add-work-unit.sh"
      ],
      "fix": "this session — plan_rebuild_goal_progress now saves the old tracker, recreates it, then carries each status across by step name via a two-file awk pass; prints the same rebuilt-progress note remove-work-unit.sh already used",
      "verification": "Empirically: completed step stays completed after an add on the fixed code; the same sequence on the pre-fix code (git stash) resets to incomplete — the exact B38 signature. Mutation-verified both directions.",
      "found_by": "user session on health-app plans; long-form report in planning-skill-defects-report.md (Trap A)",
      "notes": null,
      "created_at": "2026-08-25T09:23:27Z",
      "updated_at": "2026-08-25T10:01:13Z"
    },
    {
      "id": "B41",
      "title": "plan_git_snapshot silently no-ops when the plans root is a gitignored project .plans directory",
      "status": "not-a-defect",
      "severity": "major",
      "priority": "high",
      "parent": "B40",
      "reproduce": "In a repo with /.plans gitignored: run ~14 mutating planning helpers against one plan, then git -C .plans status --porcelain — every touched file is uncommitted; plan-document-lib.sh:61-68 guarded on [ -d \"$plan_dir/.git\" ] found no repo at plan level",
      "observed": "Every snapshot silently no-ops; the skill guarantee that every mutating helper commits first is false in this layout",
      "expected": "The snapshot resolves the enclosing plans-root repository (the same layout create-plan.sh documents) and commits there, or states plainly that snapshots are unavailable in this layout",
      "mechanism": "The guard tests only for a repo inside plan_dir itself, so the documented plans-root-repo fallback (whole plans tree versioned) never engages when .plans is gitignored from the outer repo",
      "surfaces": [
        "planning/scripts/plan-document-lib.sh"
      ],
      "fix": "Already fixed on current HEAD: create-plan.sh correctly initializes a git repo at the plans root when .plans is gitignored, writes PLAN_SNAPSHOT_REPO to .env, and plan_git_snapshot resolves via plan_snapshot_repo (.env-based, not [ -d $plan_dir/.git ]). Empirically confirmed: a mutation after create-plan produces a snapshot commit in the plans-root repo.",
      "verification": "Sandbox test: gitignored .plans inside an outer repo; create-plan + add-goal produced two commits in the plans-root git repo (initial + pre-mutation snapshot). The reporting session likely used an older installed skill version predating the PLAN_SNAPSHOT_REPO pin.",
      "found_by": "user session; proven independently via git -C .plans status after 14 mutations (report in planning-skill-defects-report.md, Trap A tail)",
      "notes": null,
      "created_at": "2026-08-25T09:24:00Z",
      "updated_at": "2026-08-25T10:06:45Z"
    },
    {
      "id": "B43",
      "title": "update-adversarial-review consumes adversarial-review-incoming even when findings arrive via --file",
      "status": "not-a-defect",
      "severity": "major",
      "priority": "high",
      "parent": null,
      "reproduce": "Place ten un-consumed rows in adversarial-review-incoming.md, then run update-adversarial-review.sh $P --file other.csv; the incoming file is rm -fed unconditionally",
      "observed": "The incoming file vanished; only a manual aside-and-restore saved the ten cycle-18 rows",
      "expected": "--file means findings come from the named file; the incoming document must be left untouched unless it was the consumed source",
      "mechanism": "The consumer removes the incoming document unconditionally at the end of a successful run without checking whether it was actually this run source",
      "surfaces": [
        "planning/scripts/update-adversarial-review.sh"
      ],
      "fix": "Not reproducible on current HEAD: with --file, consumed_incoming remains 0 and rm -f is skipped. Empirically verified with a live two-file test.",
      "verification": "Sandbox test: create plan + review + incoming file + separate CSV; run update-adversarial-review.sh --file other.csv; incoming file survives.",
      "found_by": "user session during cycle-18 landing (report in planning-skill-defects-report.md, collateral C1)",
      "notes": " Also re-mints fix-keys.json under a fresh session on every landing (collateral C2): stale keys, fresh claims required — by design but worth knowing.",
      "created_at": "2026-08-25T09:26:12Z",
      "updated_at": "2026-08-25T09:51:31Z"
    },
    {
      "id": "B14",
      "title": "The documented renders sort ids as strings, so item 10 lands between item 1 and item 2",
      "status": "fixed",
      "severity": "minor",
      "priority": "high",
      "parent": null,
      "reproduce": "jq -r 'def prank: {urgent:0,high:1,normal:2,low:3,someday:4}[.priority]//5; def srank: {blocking:0,major:1,minor:2,cosmetic:3}[.severity]//4; [.bugs[]] | sort_by(prank, srank, .id)[] | .id' BUGS.json",
      "observed": "B2 B10 B13 B3 B4 B5 B7 B8 B9 B11 B12 B6 B1 - B10 sits between B2 and B3. The todo queue renders T13 T14 T15 before T21, and T5 T6 after T31.",
      "expected": "Numeric order within a priority band: B2 B3 B4 B5 B6 B7 ... B13.",
      "mechanism": "Seven snippets across the two SKILL.md files sort with sort_by(rank, .id) or sort_by(prank, srank, .id), where .id is a string, so jq compares byte-wise and \"B10\" < \"B3\". jq has no natural-sort builtin.",
      "surfaces": [
        "todo/SKILL.md",
        "bug-report/SKILL.md"
      ],
      "fix": "def idkey: [(. | scan(\"[0-9]+\") | tonumber)?, .]; added beside the existing rank defs in all seven snippets across both SKILL.md files, and every sort_by now passes (.id | idkey). Ids stay free-form, so nothing had to be renumbered and the commit messages that reference T30 and B8 still resolve. The id row in both field tables says why the def is there, so it does not get removed as noise.",
      "verification": "The documented snippets were extracted from SKILL.md and run verbatim against the real registers: top-level normal tasks read T1 T2 T3 T4 T5 T6 T13 T14 T15 T16 T17 T21 T23 T24 T26 T27 T30 T31, and a sub-task renders under its parent. Mutation: dropping idkey from one snippet gives T1 T13 T14 T15 T16 T17 T2 T21 ... T3 T30 T31 T4 T5 T6. A first mutation probe looked green because within the high band the ids happen to sort identically either way -- the normal band is where it shows.",
      "found_by": "a bug report written against 1.4.2 by an earlier session (bug-report-old-version.md), re-verified against the current registers",
      "notes": "Live, and it produced misleading output all through this session without being noticed - every register render printed above is in the wrong order. Two candidate fixes. Zero-padded ids make lexical sort correct with no snippet change, but require renumbering ids that commit messages already reference (T30, B8), because a mix of T5 and T05 sorts wrong anyway. A derived key - def idkey: [(. | scan(\"[0-9]+\") | tonumber)?, .] - keeps ids free-form and is the only option that also respects the skills own T1 -> T1a convention, placing a child immediately after its parent in a flat render; length-then-lexical looked cheaper but drifts T1a to the end. Its stated cost, a def copied into seven snippets, is already the pattern in these files: the rank and prank/srank defs are duplicated across all of them today, so this adds no new kind of debt.",
      "created_at": "2026-08-21T09:59:41Z",
      "updated_at": "2026-08-21T10:04:58Z"
    },
    {
      "id": "B32",
      "title": "test-install-ui fails after the any-of groups: the UI parts call runtime_requirement_label without sourcing where it lives",
      "status": "fixed",
      "severity": "minor",
      "priority": "high",
      "parent": null,
      "reproduce": "bash planning/tests/test-install-ui.sh   # at e6492f6",
      "observed": "installer/src/36-ui-render.sh: line 174: runtime_requirement_label: command not found, twice, and the test reports FAIL while its own layout assertions still pass.",
      "expected": "The suite is green, so the ubuntu leg and the PR are not red for a test-harness reason.",
      "mechanism": "e6492f6 gave the UI parts a dependency they did not have: 36-ui-render.sh:175 and 37-ui-input.sh:191 now call runtime_requirement_label, which is defined in installer/src/20-runtime-tools.sh:54. test-install-ui sources the UI parts in isolation, so the function is absent there. The shipped installer is unaffected -- install.sh is one file and defines it at line 660, and installer/build.sh --check passes -- so this is a test-harness gap rather than a defect in the feature.",
      "surfaces": [
        "planning/tests/test-install-ui.sh",
        "installer/src/36-ui-render.sh",
        "installer/src/37-ui-input.sh"
      ],
      "fix": "4befd4e — test-install-ui stands in runtime_requirement_label (and the hint helper) at both sourcing sites, matching how it already stood in runtime_tool_verify",
      "verification": "test-install-ui passes locally and on both verification legs after 4befd4e; the command-not-found lines are gone from the run output.",
      "found_by": "the 5-minute watch on the -ox branch, running the gates that e6492f6 touches",
      "notes": "Cheapest fix is for the test to source 20-runtime-tools.sh alongside the UI parts, since labelling a requirement genuinely belongs there. Worth deciding whether the UI parts should depend on it at all: if the isolation the test relies on is intentional, the label belongs with the UI. Not verified beyond the failure itself -- the behavioural claim of T39, one warning naming the member set when no runtime resolves and silence when one does, was not exercised here.",
      "created_at": "2026-08-24T12:47:21Z",
      "updated_at": "2026-08-24T14:45:05Z"
    },
    {
      "id": "B35",
      "title": "The new any-of test invokes bash by path lookup, tripping the portability gate it was added alongside",
      "status": "fixed",
      "severity": "minor",
      "priority": "high",
      "parent": null,
      "reproduce": "bash planning/tests/test-portability-contract.sh   # at 4600a0b or later",
      "observed": "portability: ./planning/tests/test-installer-any-of.sh uses banned construct 'bash-by-path-lookup' at line 168 — see PORTABILITY.md. Line 168 is `if bash \"$repo_dir/installer/build.sh\" --check`, inside a two-attempt loop.",
      "expected": "A test added with a feature does not break a gate the suite already enforces.",
      "mechanism": "The rule exists because a bare `bash` resolves through PATH, so the child is a different bash than the parent and set -e and pipefail semantics differ between the two halves of one script -- which is exactly what the bash 3.2 leg is for. The repo's convention is \"$BASH\", the running shell's own path. The new test uses the bare form once.",
      "surfaces": [
        "planning/tests/test-installer-any-of.sh"
      ],
      "fix": "4befd4e — test-installer-any-of invokes installer/build.sh through \"$BASH\" like every sibling test",
      "verification": "test-portability-contract names no bash-by-path-lookup finding on the current tree; the detector regex still fires on the pre-fix line (reproduced from CI output before fixing).",
      "found_by": "the 5-minute watch, gating ox's 4600a0b: the new test itself passes, and it makes test-portability-contract fail a second way on top of B33",
      "notes": "One character of fix -- \"$BASH\" for bash -- and it is worth doing separately from B33 so the portability gate goes green in two identifiable steps rather than one ambiguous one. Worth flagging alongside it: 6f8f426 closes T39 in the register while the suite is red on B32, B33 and this, so the queue now says done for work whose gates do not pass. The register entry is not wrong about the feature landing; it is that a closure and a green suite have come apart, which is the state a reviewer would want named rather than discovered.",
      "created_at": "2026-08-24T13:13:39Z",
      "updated_at": "2026-08-24T14:14:22Z"
    },
    {
      "id": "B42",
      "title": "Atomicity checkboxes are mandatory, unwritable by any helper, and byte-exact so honest annotations fail the gate",
      "status": "fixed",
      "severity": "minor",
      "priority": "high",
      "parent": "B38",
      "reproduce": "On any step: update-plan-content.sh -ss $P 01-alpha/01-step-one atomicity-check -p 8.1: x refuses (not a mutable section); tick a box with an appended NOTE about a multi-file commit and validate-plan fails has not confirmed target isolation via grep -Fqx",
      "observed": "Boxes are written pre-ticked at creation, no helper can change them, and the validator requires all three byte-exact — every step in every valid plan asserts atomicity regardless of reality; plan-content.sh renders them to reviewers as if they were evidence",
      "expected": "Either a writable step/atomicity-check section whose recorded violations FAIL validation honestly, or deletion of the three lines in favour of the mechanical checks; boxes should start unticked so a tick is an act",
      "mechanism": "The three ticks have no plan_section_spec entry and no writer anywhere; validate-plan.sh:765-767 greps whole-line fixed strings, making the ticked trio a constant the suite certifies whether or not atomicity held (live case: AR-157, W50 ticked single-file while its commit changed seven files)",
      "surfaces": [
        "planning/scripts/plan-document-lib.sh",
        "planning/scripts/add-work-unit.sh",
        "planning/scripts/validate-plan.sh",
        "SKILL.md"
      ],
      "fix": "Goal 02 of harden-plan-data-parsing (ce8600f): unticked default at creation, update-step auto-tick from git-diff evidence with VIOLATION annotations, validator accepts annotated ticks and fails unticked-on-completed only",
      "verification": "test-atomicity-flow.sh end-to-end pins on a real git fixture (unticked default, exact-match tick, violation annotation); suite 104/104; all CI legs green",
      "found_by": "user session during cycle-18 stale-phrase fixes (report in planning-skill-defects-report.md, Trap B)",
      "notes": " Reported as medium on the four-point user scale; mapped to minor here (register enum has no medium).",
      "created_at": "2026-08-25T09:24:44Z",
      "updated_at": "2026-08-26T20:16:45Z"
    },
    {
      "id": "B1",
      "title": "A second --skill silently discards the first, so the installer installs something other than what was asked",
      "status": "fixed",
      "severity": "major",
      "priority": "normal",
      "parent": null,
      "reproduce": "bash install.sh --skill todo --skill bug-report --target /tmp/two --yes; ls /tmp/two",
      "observed": "Selected skills: bug-report — and only bug-report is installed. Reversing the two flags installs only todo. No warning either way.",
      "expected": "Both skills installed, or a refusal naming the conflict. Discarding an argument the caller supplied is the one thing it must not do silently.",
      "mechanism": "installer/src/10-cli-args.sh:60 assigns SKILL_SELECTION=\"$2\" as a scalar, so each --skill overwrites the previous. --all takes the array path; --skill never did.",
      "surfaces": [
        "installer/src/10-cli-args.sh",
        "installer/src/40-selection.sh",
        "installer/src/05-config.sh"
      ],
      "fix": "--skill accumulates: 10-cli-args.sh joins repeated values with a comma, which is what select_skills already splits, so the repeated and comma spellings meet in one place. select_skills also learns `all` as a token, so --skill all --skill todo is not a contradiction resolved by ordering. The bare 6 stays whole-string only: with seven skills 6 is also a position, and reading it as all inside a list would install one thing when a list was asked for.",
      "verification": "test-installer-skill-selection.sh asserts against what was installed, not the Selected skills line: two flags install both in either order, three install three, the comma and repeated forms agree, mixing them works, a repeat collapses, a menu number combines with a name, all wins wherever it appears, the bare 6 still means all, and the three refusals are unchanged. Mutations: restoring last-one-wins fails the first two assertions; dropping the all token fails the combined case.",
      "found_by": "installing the two register skills for the user in one command",
      "notes": "Closed as not-a-defect first, because README.md documented last-one-wins and the comma form worked. That was right about the documentation and wrong about the behaviour being acceptable: silently discarding a supplied argument is a defect whatever the manual says, and the manual was the thing to change. The status moved from not-a-defect to fixed rather than a second entry being opened, so the history stays in one place.",
      "created_at": "2026-08-20T15:44:50Z",
      "updated_at": "2026-08-21T11:00:42Z"
    },
    {
      "id": "B27",
      "title": "test-portability-contract fails on committed HEAD: the chat runtime probes name python3 without an allowlist row, so the whole suite reads red",
      "status": "fixed",
      "severity": "major",
      "priority": "normal",
      "parent": null,
      "reproduce": "git checkout 9b5e502 -- . && ./run-tests.sh   # or: nix develop -c bash planning/tests/test-portability-contract.sh on any tree containing chat/",
      "observed": "portability: ./chat/tests/test-chat.sh uses banned construct 'python3-shipped' at line 26; same for ./chat/scripts/chat-server.sh line 59; Total: Failed: 1.",
      "expected": "Files that probe an optional runtime chain must name what they may pick and are exempted like the stat-format/sha256-tool probes, keeping the suite green at HEAD.",
      "mechanism": "Assertion 4 greps comment-stripped sources against portability-rules.json detectors; chat-server.sh and its test list python3 in their python3->node->perl->socat fallback (chat/requires.tsv declares it soft), but in_allowlist() gained no chat rows when the skill landed.",
      "surfaces": [
        "planning/tests/test-portability-contract.sh",
        "chat/scripts/chat-server.sh",
        "chat/tests/test-chat.sh"
      ],
      "fix": "This change — two exact-file allowlist rows for python3-shipped with the probe rationale commented; PORTABILITY.md regenerated last per contract 7.",
      "verification": "test-portability-contract PASSes on the working tree; the pre-fix output above is the mutation (removing the rows restores both findings).",
      "found_by": "./run-tests.sh while verifying B26 — the failure predates this session's changes and sits on committed HEAD",
      "notes": "Nothing else in the suite covered the chat landing's interaction with the portability gate; the suite had not been run between the chat commits and this check.",
      "created_at": "2026-08-23T21:31:00Z",
      "updated_at": "2026-08-23T21:31:00Z"
    },
    {
      "id": "B28",
      "title": "The opencode permission autoset looks only at opencode.json, so a machine whose config is opencode.jsonc gets no grant and a commented one would have been stripped",
      "status": "fixed",
      "severity": "major",
      "priority": "normal",
      "parent": null,
      "reproduce": "home=$(mktemp -d); mkdir -p $home/.config/opencode; printf '{\"$schema\":\"https://opencode.ai/config.json\"}\\n' > $home/.config/opencode/opencode.jsonc; HOME=$home ./install.sh --skill planning --target $home/.config/opencode/skills --yes",
      "observed": "\"opencode: no $HOME/.config/opencode/opencode.json found; skipped\" — the consented grant is written nowhere and every planning helper stays behind a prompt.",
      "expected": "The editor resolves the config that exists (.json preferred over .jsonc), creates a minimal opencode.json when neither exists, and refuses to rewrite a file strict jq cannot parse instead of rebuilding it from {}.",
      "mechanism": "OPENCODE_CONFIGFILE defaulted to a hardcoded opencode.json path and a missing file returned early; separately, doc=\"$(jq '.')\" on JSON-C comments yields empty so doc='{}' fed the rewrite — stripping every comment the schema explicitly allows.",
      "surfaces": [
        "installer/src/70-permissions.sh"
      ],
      "fix": "This change — opencode_configfile() resolves override/.json/.jsonc/create-.json; a non-empty non-strict-JSON target prints manual instructions instead of being rebuilt; the fresh-created file skips its own backup.",
      "verification": "planning/tests/test-installer-opencode-permissions.sh pins all four behaviours and each written config loads under `opencode debug config`. Mutation: forcing the old single-path resolution fails three assertions (grant missing from jsonc, stray opencode.json created, refusal notice gone).",
      "found_by": "User asked to install these skills into opencode; reproduced in a sandbox HOME mirroring this machine's opencode.jsonc-only setup",
      "notes": "Went unnoticed because no test drove the opencode branch of the permission step, and development machines happened to have opencode.json. The claude editor keeps its rebuild-from-{} behaviour for settings.json, which has no JSON-C allowance.",
      "created_at": "2026-08-23T21:30:00Z",
      "updated_at": "2026-08-23T21:30:00Z"
    },
    {
      "id": "B29",
      "title": "test-mermaid-accuracy fails under the flake browser: chromium sandbox blocks mmdc, and every sequenceDiagram loses puppeteer render race",
      "status": "fixed",
      "severity": "major",
      "priority": "normal",
      "parent": null,
      "reproduce": "nix develop -c bash planning/tests/test-mermaid-accuracy.sh   # with mmdc provided by flake.nix",
      "observed": "10x \"mmdc rejected the diagram ... Failed to launch the browser process ... SUID sandbox helper binary\" without flags; with --no-sandbox, exactly the sequenceDiagram blocks fail with \"svg element not in render tree\" while flowcharts render. CI mermaid-render renders the same diagrams green.",
      "expected": "The render gate passes where the diagrams are valid, reports UNCONFIGURED when no browser can launch here, and still rejects genuinely invalid diagrams.",
      "mechanism": "Two stacked environment mismatches. (1) mmdc launched chromium with its default sandbox, impossible in this container/root context -- ci.yml already solves that for CI. (2) The nixpkgs mermaid-cli/chromium pair has a version skew: puppeteer evaluation loses the render race for every sequenceDiagram regardless of content, proven by minimal controls (flowchart TD A-->B renders; sequenceDiagram A->>B hi fails deterministically).",
      "surfaces": [
        "planning/tests/test-mermaid-accuracy.sh"
      ],
      "fix": "this session -- per-render --no-sandbox/--disable-dev-shm-usage with an isolated chromium profile dir; two control probes classify the renderer before its verdicts are trusted; the known skew signature on a sequenceDiagram downgrades to WARN only while the control pair reproduces it, everything else still fails",
      "verification": "Suite leg green locally. Mutations: a broken flowchart still FAILs (UnknownDiagramError reported); a broken sequenceDiagram still FAILs (parse error, different signature, not masked by the skew tolerance); removing the no-sandbox flags reproduces the original browser-launch failure the UNCONFIGURED branch now names.",
      "found_by": "./verify-both-shells.sh baseline run during B16 verification -- the failure was invisible until the dev flake supplied mmdc to the bash 3.2 leg",
      "notes": "CI remains the syntax authority: it brings its own browser via npx and has always rendered these blocks. The local probes exist so the suite is green in the environment flake.nix declares without weakening rejection of real defects.",
      "created_at": "2026-08-24T08:47:14Z",
      "updated_at": "2026-08-24T08:47:14Z"
    },
    {
      "id": "B6",
      "title": "A benchmark test calls timeout, which macOS does not have",
      "status": "fixed",
      "severity": "minor",
      "priority": "normal",
      "parent": null,
      "reproduce": "CI run 32394476430, macos-latest: benchmark/planning/tests/test-review-lifecycle.sh line 492",
      "observed": "timeout: command not found, after every earlier assertion in the file had passed.",
      "expected": "Either a portable timeout, or the test degrading honestly on a platform without one.",
      "mechanism": "GNU coreutils timeout is not on macOS. PORTABILITY.md already lists it under gnu-only-tools with a gtimeout fallback; this call site predates the rule or was missed because the gate does not scan benchmark tests the same way.",
      "surfaces": [
        "benchmark/planning/tests/test-review-lifecycle.sh"
      ],
      "fix": "Both `timeout 60s` wrappers removed rather than given a gtimeout fallback. What they wrapped is a shell script in the fixture that writes a file and exits, so the cap could only fire on a defect and would then report a timeout instead of the defect.",
      "verification": "The test passes and takes 5 seconds against a 60s cap per call. It was the only test in an 80-test suite with a wall clock, and the only reason this file could not run on macOS.",
      "found_by": "the macOS leg of CI",
      "notes": "Asked directly: why a timeout at all. lib-agent.sh does bound a real agent and handles timeout/gtimeout properly with an explicit refusal — that one is a genuine need, these two were a hedge. A `sleep 1` remains in the codex-worker fake, with a stated reason: the runner must capture a live process group. That is a real synchronisation point and wants a handshake rather than a sleep; recorded as T29.",
      "created_at": "2026-08-20T17:17:13Z",
      "updated_at": "2026-08-20T17:27:05Z"
    },
    {
      "id": "B11",
      "title": "create-plan.sh creates the plans root before validating the plan name, so a refused call leaves a directory behind",
      "status": "fixed",
      "severity": "minor",
      "priority": "normal",
      "parent": null,
      "reproduce": "bash planning/scripts/create-plan.sh /tmp/mkdir-probe/BadName title; ls -d /tmp/mkdir-probe",
      "observed": "Refuses with \"Plan directory name must be kebab-case\", and /tmp/mkdir-probe exists afterwards.",
      "expected": "Nothing on disk changes when the call is refused.",
      "mechanism": "The mkdir -p added in 9f41091 sits in the argument case around line 53; the kebab-case check and the refuse-existing check are at 63 and 65. CODE-CONTRACTS.md contract 2 puts anything irreversible last.",
      "surfaces": [
        "planning/scripts/create-plan.sh"
      ],
      "fix": "The mkdir moved out of the argument case to just after the guards -- the kebab-case check, the refuse-existing check and plan_require_safe_value -- and is now conditional on the directory being absent. An explicit path still creates the directory that holds it; a call about to be refused no longer does.",
      "verification": "test-create-plan-explicit-root.sh gains two assertions: a non-kebab name is refused with 64 and its parent does not exist afterwards, and an occupied plan directory is refused with 73. Mutation: moving the mkdir back into the argument case fails the first with \"a refused call created its parent directory\". test-coverage-gaps and test-plan-commands still pass, which is what exercises the bare-name path that does not create anything itself.",
      "found_by": "reviewing 9f41091",
      "notes": "The mkdir itself is right - an explicit path should be able to create its parent. Only the order is wrong: move it after both guards.",
      "created_at": "2026-08-21T09:34:37Z",
      "updated_at": "2026-08-21T10:15:27Z"
    },
    {
      "id": "B12",
      "title": "The planning-bugs.json plan artifact was added with no writer and no diagram update",
      "status": "fixed",
      "severity": "minor",
      "priority": "normal",
      "parent": null,
      "reproduce": "grep -rn planning-bugs --include=*.sh planning/scripts",
      "observed": "Readers everywhere - plan_document_path, plan_document_kind, plan-context-lib, the plan-content usage text, test-document-id-parity - and nothing that creates the file. Only the test writes one.",
      "expected": "Either a script that produces it, or a stated reason it is written by an agent by hand.",
      "mechanism": "The id was wired through every reader consistently, which is the hard part; the producer and the documentation were not. MAINTAINER.md section 4 step 4 requires the ARCHITECTURE.md diagram to be updated in the same change when a plan artifact is added, and that file is untouched.",
      "surfaces": [
        "planning/ARCHITECTURE.md",
        "planning/scripts/lib/document/plan_document_path.sh",
        "planning/scripts/plan-context-lib.sh"
      ],
      "fix": "add-planning-bug.sh writes the register, in the shape add-fix-claim.sh established when fixes.md needed the same thing: validate everything before touching the file, snapshot the plan, append through plan_atomic_write. The file follows the bug-report skill schema, so that skill jq recipes read a plan register unchanged. Registered in PACKAGE-MANIFEST.txt, PACKAGE-MAP.tsv and the prod arm of skill_files, and both the artifact and its writer are now in the section 5 diagram, which MAINTAINER.md step 4 requires.",
      "verification": "test-add-planning-bug.sh: the register does not exist until written and asking for it gives 66; first use creates it and the entry is read back rather than trusted from an exit code; a backslash and embedded quotes survive; six refusals each with their own code (73 duplicate, 64 bad id, 64 missing reproduction, 64 unknown severity, 64 status fixed, 66 missing plan, 65 damaged register) and the damaged file is left untouched; and the bug-report skill own render lists both entries. Mutations caught: dropping the duplicate check, widening the status enum, appending to a damaged register. All 10 mermaid diagrams render with the new nodes under mmdc. The read-back guard is tested rather than merely asserted to be untestable: a jq on PATH passes every call through except the append, the one carrying --arg now, which it answers with nothing. That call then refuses with 70, prints no success line, and leaves the zero-byte register in place for inspection, with a control on a clean plan proving the seam is what makes the difference. Removing the guard now fails two assertions.",
      "found_by": "reviewing 9f41091",
      "notes": "The intent was inferred, not known: the test seeds {\"bugs\":[]}, the id is grouped with the optional artifacts, a missing file is asserted to give 66, and planning/SKILL.md never mentions it. So it reads as a plan-scoped defect register in the bug-report skill format. Writing the writer rather than documenting hand-authoring follows this repo own precedent, commit 3783dab. Building it turned up that jq handed an empty input never runs its filter, so the first version wrote a zero-byte register and reported success; the guard against that is now covered by a PATH seam rather than left as a known gap, because MAINTAINER.md step 3 asks for a test of the actionable-error path and a backstop with no test is a claim, not a guard.",
      "created_at": "2026-08-21T09:34:37Z",
      "updated_at": "2026-08-21T10:40:32Z"
    },
    {
      "id": "B17",
      "title": "verify-both-shells.sh executes a stray word, printing \"been: command not found\"",
      "status": "fixed",
      "severity": "minor",
      "priority": "normal",
      "parent": null,
      "reproduce": "Not yet isolated. Seen once in the run whose worktree base was 359e7ff: ./verify-both-shells.sh --keep printed the error between the worktree line and the bash 5.3 summary.",
      "observed": "./verify-both-shells.sh: line 106: been: command not found — then both legs ran and reported 88 passed, 0 failed, so the run itself was unaffected.",
      "expected": "No shell noise from the harness.",
      "mechanism": "Not a code path. The observed run executed the uncommitted working-tree copy while the B16 sweep block was being written into it (base 359e7ff at 11:18-11:21 UTC; the block landed as 1081e19). Bash reads scripts incrementally, so when the file was rewritten beneath the running instance it resumed mid-comment and executed a fragment as a command word. No committed revision of the harness can execute a stray word: there is no eval, and shellcheck reports no unquoted-expansion finding. A clean single-writer full run printed zero stray output.",
      "surfaces": [
        "verify-both-shells.sh"
      ],
      "fix": "this session -- a header warning on verify-both-shells.sh (never rewrite the file while a run is in flight); the code needed no change",
      "verification": "Static: no eval/unquoted execution path in any revision (shellcheck clean at error severity). Dynamic: baseline ./verify-both-shells.sh run at 3701d04 completed 95/95 on bash 5.3 with no stray output.",
      "found_by": "the verification of 359e7ff, which reported it while otherwise passing 88 of 88 on both shells",
      "notes": "Recorded to be investigated later rather than fixed on sight. Related to B16 only by file: that one is the worktree sweep deleting a live run, and its fix is in the tree unverified.",
      "created_at": "2026-08-21T11:29:11Z",
      "updated_at": "2026-08-24T08:47:14Z"
    },
    {
      "id": "B21",
      "title": "remove-work-unit.sh on a goal's last unit deletes the whole numbered Owned-work-units roster instead of leaving the empty scaffold, which breaks the next add",
      "status": "fixed",
      "severity": "minor",
      "priority": "normal",
      "parent": null,
      "reproduce": "Plan with one goal owning exactly one work unit; run remove-work-unit.sh <plan> W01; inspect the goal's Owned work units section; then run add-work-unit.sh for the same goal.",
      "observed": "All §9.x paragraphs are gone (heading and testing-requirement table remain). The subsequent add-work-unit.sh refuses with \"Goal has no numbered Owned work units section\".",
      "expected": "An emptied roster returns to the just-created shape: a § 9.1 holding '<add work units with add-work-unit.sh>'.",
      "mechanism": "plan_rewrite_owned_work_units emits one §9.N paragraph per remaining inventory row and nothing at all when none remain; add-work-unit.sh's placeholder branch keys on the literal '<add work units with add-work-unit.sh>' line, which no longer exists anywhere in the section.",
      "surfaces": [
        "planning/scripts/plan-reconcile-lib.sh"
      ],
      "fix": "plan_rewrite_owned_work_units emits the created scaffold when the goal has zero remaining rows: a § 9.1 holding <add work units with add-work-unit.sh>, the registered placeholder add-goal.sh writes and add-work-unit.sh replaces. Survivors keep their renumbered §9.N labels; only the empty case gained the scaffold.",
      "verification": "planning/tests/test-owned-roster-scaffold.sh: against HEAD removing the last unit left no §9.1 and no placeholder, and the next add refused with 64; with the fix the scaffold survives, the re-add succeeds end to end, and the control proves survivors are relabelled without a stray placeholder. Mutation: whole-revert to HEAD reproduces all five failures.",
      "found_by": "ox-alpha exercising the planning skill on the health-app VPS (bugs-found-by-ox-alpha.md #4)",
      "notes": "This manufactures the precondition of B20. The registered placeholder token is already in placeholders.json and is what add-goal.sh emits, so restoring it keeps the roster in a shape every reader already knows.",
      "created_at": "2026-08-22T16:21:07Z",
      "updated_at": "2026-08-22T19:55:28Z"
    },
    {
      "id": "B26",
      "title": "Nothing in the execution-phase docs names the sanctioned way to mark a step completed, so executors hand-sed the generated tracker",
      "status": "fixed",
      "severity": "minor",
      "priority": "normal",
      "parent": null,
      "reproduce": "Read planning/roles/execution.md as a worker or coordinator looking for how to record a finished step: it says Alex updates progress trackers and nothing else. Grep that doc for update-step or completed: no hits.",
      "observed": "The health-app executor hand-edited <goal>/progress.md rows with sed about 40 times across two plans, flipping '| incomplete |' cells to '| completed |'. The helper they asked for (complete-step.sh) already exists as update-step.sh <goal-directory> <step-name> <incomplete|in-progress|completed>, installed and listed in the SKILL.md command reference - just not reachable from the doc an executor reads during execution.",
      "expected": "The phase-scoped execution doc routes tracker updates to the helpers and states that the trackers are generated: hand-editing status cells or bars is damage.",
      "mechanism": "MAINTAINER rule 2.2 scopes agents to the doc for their task; the execution roster names the responsibility but not the mechanism, and the two relevant lines sit in SKILL.md's command-reference wall of roughly fifty commands. A capability that exists but is unreachable from the reader position fails as if absent.",
      "surfaces": [
        "planning/roles/execution.md"
      ],
      "fix": "roles/execution.md gained an Updating-trackers section: update-step.sh per step (row rewrite plus bar re-derivation), update-plan-progress.sh at plan level, and the explicit statement that hand-editing either tracker is damage with no sanctioned spelling. No new helper: their suggested complete-step.sh is update-step.sh, and a second name would be dedupe debt.",
      "verification": "The routing gap is grep-provable on the pre-fix doc (no update-step/completed hits in roles/execution.md) and closed by the new section naming both helpers verbatim. test-persona-drift and test-voice-artifact-drift pass unchanged; the file keeps its MODE marker and its registered manifest row, so no packaging surface moved.",
      "found_by": "health-app worker finding #7 in bugs-found-by-ox-alpha.md and ../health-app/bug-fixed/fixed-by-ai-skills.md (2026-08-22)",
      "notes": "Deliberately NOT building their suggested complete-step.sh: it would duplicate update-step.sh, which the dedupe ratchet exists to prevent. The fix is the missing route plus the prohibition. The worker sed edits were semantically valid (the status cell is the on-disk contract) - the mechanics were the violation.",
      "created_at": "2026-08-22T21:35:13Z",
      "updated_at": "2026-08-22T21:35:54Z"
    },
    {
      "id": "B44",
      "title": "Cycle numbering breaks after gaps: archived as Cycle 11 following Cycle 16",
      "status": "not-a-defect",
      "severity": "minor",
      "priority": "normal",
      "parent": null,
      "reproduce": "With history containing cycles up to 16 but gaps in numbering, land findings via update-adversarial-review.sh; the prior table is archived under a heading numbered grep-count-plus-one",
      "observed": "Prior table archived as Cycle 11 after Cycle 16; confirmed in adversarial-review-history.md",
      "expected": "The new cycle number is one greater than the highest existing cycle number, not the count of headings",
      "mechanism": "update-adversarial-review.sh computes the next cycle as grep -c of Cycle headings plus one; heading numbers are not contiguous once any historical gap exists",
      "surfaces": [
        "planning/scripts/update-adversarial-review.sh"
      ],
      "fix": "Already fixed: update-adversarial-review.sh uses highest_cycle_number() (max + 1, never a count) since before this report was filed; the comment at line 74 documents the exact failure mode described.",
      "verification": "Read the code: highest_cycle_number takes max of all Cycle headings, not their count. The observed Cycle-11-after-16 came from an older installed skill version.",
      "found_by": "user session during cycle-18 landing (report in planning-skill-defects-report.md, collateral C3)",
      "notes": null,
      "created_at": "2026-08-25T09:46:07Z",
      "updated_at": "2026-08-25T09:53:29Z"
    },
    {
      "id": "B25",
      "title": "Reviewer prose cannot quote table syntax: a pipe anywhere in a CSV cell refuses the findings write, and \\| does not survive either",
      "status": "fixed",
      "severity": "minor",
      "priority": "low",
      "parent": null,
      "reproduce": "Feed a finding row whose prose cell contains a literal | (or \\|) through update-adversarial-review.sh.",
      "observed": "plan_render_csv_table refuses: the cell contains a pipe character that would break the Markdown table. An escaped \\| matches the same post-parse /\\|/ test, so no spelling of a pipe reaches a cell today.",
      "expected": "Either an escape spelling that lands a literal pipe in the rendered cell, or documented wording guidance for referencing table rows without pipes.",
      "mechanism": "The renderer rejects any parsed field containing a pipe before emitting the row; parse_csv knows no backslash-pipe escape, only doubled quotes for quotes.",
      "surfaces": [
        "planning/scripts/lib/table/plan_render_csv_table.sh",
        "planning/scripts/update-adversarial-review.sh"
      ],
      "fix": "this session — emit_row strips \\\\-escapes before testing for a raw pipe, so a cell spelled with the GFM escape renders verbatim and only an unescaped pipe is refused; refusal message and REVIEWER.md document the spelling",
      "verification": "test-csv-table-errors: escaped pipe renders as | it\\\\|em |; raw pipe still refused naming column; mixed escaped+raw row still refused at the raw column. Mutation: reverting emit_row to reject any pipe fails the escaped-spelling assertion.",
      "found_by": "health-app worker minor note in ../health-app/bug-fixed/fixed-by-ai-skills.md (2026-08-22): reviewer prose quoting table syntax needs guidance",
      "notes": "Recorded so it is not lost; needs a small design decision (add an escape to the shared renderer vs authoring guidance) before fixing. The CRLF half of the same note is already covered: LF-only is documented and the refusal names the conversion.",
      "created_at": "2026-08-22T20:49:54Z",
      "updated_at": "2026-08-24T09:24:39Z"
    },
    {
      "id": "B45",
      "title": "--file rejects CRLF CSVs with a misleading column-count message",
      "status": "not-a-defect",
      "severity": "minor",
      "priority": "low",
      "parent": null,
      "reproduce": "Feed a csv.writer-produced CRLF findings file via update-adversarial-review.sh --file; refusal says CSV table must have 5 columns",
      "observed": "plan_render_csv_table exits 4 on the carriage return but the caller reuses the column/pipe message, so the reader debugs the wrong problem",
      "expected": "The CRLF diagnosis (convert to LF) reaches the caller exactly as it does on stdin",
      "mechanism": "The caller maps every nonzero renderer exit to its own generic message instead of passing the renderer-specific text through",
      "surfaces": [
        "planning/scripts/update-adversarial-review.sh"
      ],
      "fix": "Not reproducible on current HEAD: plan_render_csv_table exit 7 names CRLF and says Convert it to LF. Empirically verified with a CRLF fixture via --file.",
      "verification": "Sandbox test with a real CRLF CSV through --file produced the correct CRLF-specific message on stderr.",
      "found_by": "user session (report in planning-skill-defects-report.md, collateral C4)",
      "notes": null,
      "created_at": "2026-08-25T09:46:07Z",
      "updated_at": "2026-08-25T09:53:29Z"
    }
  ]
}
