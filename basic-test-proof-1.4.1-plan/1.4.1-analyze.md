# Basic test proof analysis: revision 1.4.1

## Execution record

- Revision: 1.4.1
- Start: 2026-08-07T23:48:38+02:00
- End: 2026-08-07T23:48:40+02:00
- Elapsed seconds: 2
- Worker result: the carried-forward button-chain planning proof was revalidated under 1.4.1; future HTML/browser execution remains intentionally deferred.
- Execution mode: sequential; no subagent, browser, server, or implementation process was started.

## Durable artifacts

- Plan directory: `/home/tschallacka/git/ai-skills/basic-test-proof-1.4.1-plan`
- Work units: W01–W07 across 02 goals.
- UI story: US-01 remains planning-only; browser execution is excluded by the proof boundary.
- Review: `adversarial-review.md`, carried forward from the equivalent approved proof shape.
- Progress: goal and plan trackers are present; future implementation/browser units remain incomplete by design.

## Validation evidence

- `planning/scripts/validate-plan.sh basic-test-proof-1.4.1-plan`: exit 0; `Plan validation passed: 7 work units across 2 goals.`
- `planning/tests/test-plan-context.sh --audit-triggers` via the resource-limited wrapper: exit 0; audit scope/schema tests passed.
- `planning/tests/test-plan-context.sh --benchmark` via the resource-limited wrapper: exit 0; small 76% reduction, medium 92% reduction, coupled 91% reduction; all recommended continue.

## Safety and cleanup

- No HTML/HTM artifact was created, edited, opened, served, or tested.
- No browser or server was started.
- No persistent process was left by the proof commands.

## Token-cost evidence

No isolated 1.4.1 worker-thread token total was available for this coordinator
run. This report does not invent a billing or usage number. The measured
execution above covers the revalidation pass; it is not comparable to the
full worker telemetry totals in the prior 1.4.0 report.

## Isolated rerun status

Two fresh Codex CLI attempts were made for a fair 1.4.1 worker run:

- `019fde38-570c-7a33-8120-50f674886759` stalled in the code-mode host and was
  terminated after producing no plan, no turns, and no SQLite usage records.
- `019fde3e-b644-7611-8225-2ce27f514aca` started with a valid isolated thread
  but stalled before its first tool turn because the host reported repeated
  `Resource temporarily unavailable` process-limit failures. It was
  terminated without producing a plan, turns, or usage records.

Neither attempt is counted as a 1.4.1 benchmark result. A fair end-to-end
comparison therefore remains pending a fresh isolated worker run when the host
can start the Codex code-mode process normally.

## Comparison with prior proof

The same plan shape and context benchmark outputs were revalidated:

| Measure | 1.4.1 revalidation | 1.4.0 | 1.3.1 |
|---|---:|---:|---:|
| Plan work units | 7 | 7 | 7 |
| Goals | 2 | 2 | 2 |
| Validator | pass | pass | pass |
| Small reduction | 76% | 76% | not recorded |
| Medium reduction | 92% | 92% | not recorded |
| Coupled reduction | 91% | 91% | not recorded |
| Validation/benchmark elapsed | 2s | 121s full run | 419s full run |

The 1.4.1 result confirms that the committed skill passes the same durable
plan and context-cache proof. Because this run revalidated the carried-forward
proof artifacts instead of spawning a fresh independent planning worker, no
new end-to-end token-efficiency claim is made.

## Handoff

Future execution starts with W01, confirms the repository-root
`button-chain.html` assumption, executes W02, performs W07, then runs W03/US-01
through a real browser after the planning-only safety boundary is lifted.
