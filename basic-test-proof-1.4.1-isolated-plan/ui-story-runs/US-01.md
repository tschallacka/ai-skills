# Browser run cache: US-01

## Starting state

- Future button-chain.html loaded in a fresh rendered browser; exactly one initial button visible; no prior presses

## Buffered interaction sequence

| Order | Direct UI input | Target / value | Expected readiness signal |
|---|---|---|---|
| 1 | Mouse-click the visible current last button, wait for exactly one new button below it, and repeat until pressing generated button four | Initial button followed by generated buttons one through four; the current last visible button is always the next target | Exactly one new button becomes visible below after each pre-completion press; after generated button four, prior content is gone and finished plus a white border are visible |

## Waits and readiness

| After order | Wait or condition | Maximum wait | Observed result |
|---|---|---|---|
| 1 | Exactly one new button becomes visible below after each pre-completion press; after generated button four, prior content is gone and finished plus a white border are visible | 2 seconds per visible readiness condition | Not run yet |

## Run result

- Status: `⏭️ excluded`
- Evidence: User-approved exclusion for this planning-only proof; no HTML was opened and no browser run occurred.
- Cache validity: Created for the current planned interaction sequence; future execution must replace the exclusion with rendered evidence.
