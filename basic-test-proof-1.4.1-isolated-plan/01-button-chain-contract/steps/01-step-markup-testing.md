# Testing companion: 01-step-markup

## Browser verification

Future executor: open `button-chain.html` in a fresh rendered browser and confirm that `#button-chain` initially contains exactly one visible button. This planning proof does not create, open, or inspect the file.

## Pass/fail criteria

Pass only when the rendered starting state has exactly one visible button and no unrelated controls. Current result: deferred by the explicit planning-only boundary.
