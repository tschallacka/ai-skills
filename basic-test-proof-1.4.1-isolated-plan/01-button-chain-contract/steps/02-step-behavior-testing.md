# Testing companion: 02-step-behavior

## Browser verification

Future executor: click the initial button and then each newly appended current last button. Confirm exactly one button appears below after every pre-completion press. Press generated button four and observe cleared prior content, `finished`, and a visible white border.

## Pass/fail criteria

Pass only if the initial button is not counted as generated, each valid press appends once, and generated button four produces the exact completion state. Current result: deferred.
