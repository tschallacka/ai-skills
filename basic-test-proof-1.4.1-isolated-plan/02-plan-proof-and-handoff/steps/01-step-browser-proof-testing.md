# Testing companion: 01-step-browser-proof

## Browser verification

Future executor: run US-01 through the rendered UI using real mouse clicks and the cached readiness conditions. Do not use console evaluation, injected events, storage edits, direct API calls, or server shortcuts.

## Pass/fail criteria

Pass only with visible evidence for every single append and the final cleared document, `finished` text, and white border. Current result: explicitly excluded by the user.
