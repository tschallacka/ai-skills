#!/usr/bin/env bash
# The repo's pre-push gate: ./pre-push-check.sh (MAINTAINER.md section 4).
# Hook failure aborts the push. Bypass explicitly with --no-verify when you
# truly must; CI runs the same gates server-side regardless.
exec "$(git rev-parse --show-toplevel)/pre-push-check.sh" "$@"
